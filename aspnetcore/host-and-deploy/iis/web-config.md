---
title: Soubor web.config
author: rick-anderson
description: Seznamte se s tím, co se nachází v souboru web.config a jak nakonfigurovat různé možnosti ASP.NET Core modulu.
monikerRange: '>= aspnetcore-5.0'
ms.author: riande
ms.custom: mvc
ms.date: 02/07/2020
no-loc:
- ASP.NET Core Identity
- cookie
- Cookie
- Blazor
- Blazor Server
- Blazor WebAssembly
- Identity
- Let's Encrypt
- Razor
- SignalR
uid: host-and-deploy/iis/web-config
ms.openlocfilehash: 4d7305f7184745b66c5de6c86b907d419183cb3f
ms.sourcegitcommit: d60bfd52bfb559e805abd654b87a2a0c7eb69cf8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/06/2020
ms.locfileid: "91755277"
---
# <a name="webconfig-file"></a><span data-ttu-id="75479-103">`web.config` souborů</span><span class="sxs-lookup"><span data-stu-id="75479-103">`web.config` file</span></span>

<span data-ttu-id="75479-104">`web.config`Je soubor, který čte služba IIS a [modul ASP.NET Core](xref:host-and-deploy/aspnet-core-module) pro konfiguraci aplikace hostované službou IIS.</span><span class="sxs-lookup"><span data-stu-id="75479-104">The `web.config` is a file that is read by IIS and the [ASP.NET Core Module](xref:host-and-deploy/aspnet-core-module) to configure an app hosted with IIS.</span></span>

## <a name="webconfig-file-location"></a><span data-ttu-id="75479-105">`web.config` umístění souboru</span><span class="sxs-lookup"><span data-stu-id="75479-105">`web.config` file location</span></span>

<span data-ttu-id="75479-106">Aby bylo možné správně nastavit [modul ASP.NET Core](xref:host-and-deploy/aspnet-core-module) , `web.config` musí být soubor přítomen v [kořenové cestě obsahu](xref:fundamentals/index#content-root) (obvykle v základní cestě aplikace) nasazené aplikace.</span><span class="sxs-lookup"><span data-stu-id="75479-106">In order to set up the [ASP.NET Core Module](xref:host-and-deploy/aspnet-core-module) correctly, the `web.config` file must be present at the [content root](xref:fundamentals/index#content-root) path (typically the app base path) of the deployed app.</span></span> <span data-ttu-id="75479-107">Toto je stejné umístění jako fyzická cesta k webu poskytované službě IIS.</span><span class="sxs-lookup"><span data-stu-id="75479-107">This is the same location as the website physical path provided to IIS.</span></span> <span data-ttu-id="75479-108">`web.config`Soubor se vyžaduje v kořenu aplikace, aby bylo možné publikování více aplikací pomocí nasazení webu.</span><span class="sxs-lookup"><span data-stu-id="75479-108">The `web.config` file is required at the root of the app to enable the publishing of multiple apps using Web Deploy.</span></span>

<span data-ttu-id="75479-109">Citlivé soubory existují na fyzické cestě aplikace, například `{ASSEMBLY}.runtimeconfig.json` `{ASSEMBLY}.xml` (dokumentační komentáře XML), a `{ASSEMBLY}.deps.json` , kde zástupný symbol `{ASSEMBLY}` je název sestavení.</span><span class="sxs-lookup"><span data-stu-id="75479-109">Sensitive files exist on the app's physical path, such as `{ASSEMBLY}.runtimeconfig.json`, `{ASSEMBLY}.xml` (XML Documentation comments), and `{ASSEMBLY}.deps.json`, where the placeholder `{ASSEMBLY}` is the assembly name.</span></span> <span data-ttu-id="75479-110">Když `web.config` je soubor přítomen a lokalita se spouští normálně, služba IIS tyto citlivé soubory po vyžádání neobsluhuje.</span><span class="sxs-lookup"><span data-stu-id="75479-110">When the `web.config` file is present and the site starts normally, IIS doesn't serve these sensitive files if they're requested.</span></span> <span data-ttu-id="75479-111">Pokud `web.config` soubor chybí, nesprávně pojmenovaný nebo nedokáže nakonfigurovat lokalitu pro normální spuštění, služba IIS může obsluhovat citlivé soubory veřejně.</span><span class="sxs-lookup"><span data-stu-id="75479-111">If the `web.config` file is missing, incorrectly named, or unable to configure the site for normal startup, IIS may serve sensitive files publicly.</span></span>

<span data-ttu-id="75479-112">**Tento `web.config` soubor musí být v nasazení přítomen vždy, správně pojmenován a může nakonfigurovat lokalitu pro normální spuštění. Nikdy neodstraňujte `web.config` soubor z produkčního nasazení.**</span><span class="sxs-lookup"><span data-stu-id="75479-112">**The `web.config` file must be present in the deployment at all times, correctly named, and able to configure the site for normal start up. Never remove the `web.config` file from a production deployment.**</span></span>

<span data-ttu-id="75479-113">Pokud `web.config` soubor není v projektu přítomen, vytvoří se soubor se správnými `processPath` a `arguments` ke konfiguraci modulu ASP.NET Core a přesunutí do [publikovaného výstupu](xref:host-and-deploy/directory-structure).</span><span class="sxs-lookup"><span data-stu-id="75479-113">If a `web.config` file isn't present in the project, the file is created with the correct `processPath` and `arguments` to configure the ASP.NET Core Module and moved to [published output](xref:host-and-deploy/directory-structure).</span></span>

<span data-ttu-id="75479-114">Pokud `web.config` je v projektu přítomen soubor, soubor je transformován se správnými `processPath` a `arguments` ke konfiguraci modulu ASP.NET Core a přesunutí do publikovaného výstupu.</span><span class="sxs-lookup"><span data-stu-id="75479-114">If a `web.config` file is present in the project, the file is transformed with the correct `processPath` and `arguments` to configure the ASP.NET Core Module and moved to published output.</span></span> <span data-ttu-id="75479-115">Transformace nemění nastavení konfigurace služby IIS v souboru.</span><span class="sxs-lookup"><span data-stu-id="75479-115">The transformation doesn't modify IIS configuration settings in the file.</span></span>

<span data-ttu-id="75479-116">`web.config`Soubor může poskytovat další nastavení konfigurace služby IIS, která ovládají aktivní moduly IIS.</span><span class="sxs-lookup"><span data-stu-id="75479-116">The `web.config` file may provide additional IIS configuration settings that control active IIS modules.</span></span> <span data-ttu-id="75479-117">Informace o modulech služby IIS, které jsou schopné zpracovávat požadavky s ASP.NET Core aplikacemi, najdete v tématu [IIS modules](xref:host-and-deploy/iis/modules) .</span><span class="sxs-lookup"><span data-stu-id="75479-117">For information on IIS modules that are capable of processing requests with ASP.NET Core apps, see the [IIS modules](xref:host-and-deploy/iis/modules) topic.</span></span>

<span data-ttu-id="75479-118">Vytváření, transformace a publikování `web.config` souboru je zpracováno nástrojem MSBuild cíl ( `_TransformWebConfig` ) při publikování projektu.</span><span class="sxs-lookup"><span data-stu-id="75479-118">Creating, transforming, and publishing the `web.config` file is handled by an MSBuild target (`_TransformWebConfig`) when the project is published.</span></span> <span data-ttu-id="75479-119">Tento cíl je k dispozici v cíle webové sady SDK ( `Microsoft.NET.Sdk.Web` ).</span><span class="sxs-lookup"><span data-stu-id="75479-119">This target is present in the Web SDK targets (`Microsoft.NET.Sdk.Web`).</span></span> <span data-ttu-id="75479-120">Sada SDK je nastavena v horní části souboru projektu:</span><span class="sxs-lookup"><span data-stu-id="75479-120">The SDK is set at the top of the project file:</span></span>

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">
```

<span data-ttu-id="75479-121">Chcete-li webové sadě SDK zabránit ve transformaci tohoto `web.config` souboru, použijte `<IsTransformWebConfigDisabled>` vlastnost v souboru projektu:</span><span class="sxs-lookup"><span data-stu-id="75479-121">To prevent the Web SDK from transforming the `web.config` file, use the `<IsTransformWebConfigDisabled>` property in the project file:</span></span>

```xml
<PropertyGroup>
  <IsTransformWebConfigDisabled>true</IsTransformWebConfigDisabled>
</PropertyGroup>
```

<span data-ttu-id="75479-122">Při zakazování webové sady SDK z transformace souboru `processPath` `arguments` by měl být sada a nastavena ručně vývojářem.</span><span class="sxs-lookup"><span data-stu-id="75479-122">When disabling the Web SDK from transforming the file, the `processPath` and `arguments` should be manually set by the developer.</span></span> <span data-ttu-id="75479-123">Další informace naleznete v tématu <xref:host-and-deploy/aspnet-core-module>.</span><span class="sxs-lookup"><span data-stu-id="75479-123">For more information, see <xref:host-and-deploy/aspnet-core-module>.</span></span>

## <a name="configuration-of-aspnet-core-module-with-webconfig"></a><span data-ttu-id="75479-124">Konfigurace modulu ASP.NET Core s `web.config`</span><span class="sxs-lookup"><span data-stu-id="75479-124">Configuration of ASP.NET Core Module with `web.config`</span></span>

<span data-ttu-id="75479-125">Modul ASP.NET Core je nakonfigurován s `aspNetCore` částí `system.webServer` uzlu v `web.config` souboru lokality.</span><span class="sxs-lookup"><span data-stu-id="75479-125">The ASP.NET Core Module is configured with the `aspNetCore` section of the `system.webServer` node in the site's `web.config` file.</span></span>

<span data-ttu-id="75479-126">Následující `web.config` soubor je publikován pro [nasazení závislé na rozhraní](/dotnet/articles/core/deploying/#framework-dependent-deployments-fdd) a konfiguruje modul ASP.NET Core pro zpracování požadavků lokality:</span><span class="sxs-lookup"><span data-stu-id="75479-126">The following `web.config` file is published for a [framework-dependent deployment](/dotnet/articles/core/deploying/#framework-dependent-deployments-fdd) and configures the ASP.NET Core Module to handle site requests:</span></span>

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <handlers>
        <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <aspNetCore processPath="dotnet"
                  arguments=".\MyApp.dll"
                  stdoutLogEnabled="false"
                  stdoutLogFile=".\logs\stdout"
                  hostingModel="inprocess" />
    </system.webServer>
  </location>
</configuration>
```

<span data-ttu-id="75479-127">Následující `web.config` publikace jsou publikovány pro [samostatně uzavřené nasazení](/dotnet/articles/core/deploying/#self-contained-deployments-scd):</span><span class="sxs-lookup"><span data-stu-id="75479-127">The following `web.config` is published for a [self-contained deployment](/dotnet/articles/core/deploying/#self-contained-deployments-scd):</span></span>

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <handlers>
        <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <aspNetCore processPath=".\MyApp.exe"
                  stdoutLogEnabled="false"
                  stdoutLogFile=".\logs\stdout"
                  hostingModel="inprocess" />
    </system.webServer>
  </location>
</configuration>
```

<span data-ttu-id="75479-128"><xref:System.Configuration.SectionInformation.InheritInChildApplications%2A>Vlastnost je nastavena na hodnotu `false` , která označuje, že nastavení zadaná v rámci [`<location>`](/iis/manage/managing-your-configuration-settings/understanding-iis-configuration-delegation#the-concept-of-location) elementu nejsou děděna aplikacemi, které jsou umístěny v podadresáři aplikace.</span><span class="sxs-lookup"><span data-stu-id="75479-128">The <xref:System.Configuration.SectionInformation.InheritInChildApplications%2A> property is set to `false` to indicate that the settings specified within the [`<location>`](/iis/manage/managing-your-configuration-settings/understanding-iis-configuration-delegation#the-concept-of-location) element aren't inherited by apps that reside in a subdirectory of the app.</span></span>

<span data-ttu-id="75479-129">Když je aplikace nasazená na [Azure App Service](https://azure.microsoft.com/services/app-service/), `stdoutLogFile` cesta je nastavená na `\\?\%home%\LogFiles\stdout` .</span><span class="sxs-lookup"><span data-stu-id="75479-129">When an app is deployed to [Azure App Service](https://azure.microsoft.com/services/app-service/), the `stdoutLogFile` path is set to `\\?\%home%\LogFiles\stdout`.</span></span> <span data-ttu-id="75479-130">Cesta uloží protokoly stdout do `LogFiles` složky, což je umístění, které služba automaticky vytvořila.</span><span class="sxs-lookup"><span data-stu-id="75479-130">The path saves stdout logs to the `LogFiles` folder, which is a location automatically created by the service.</span></span>

<span data-ttu-id="75479-131">Informace o konfiguraci dílčí aplikace IIS najdete v tématu <xref:host-and-deploy/iis/index#sub-applications> .</span><span class="sxs-lookup"><span data-stu-id="75479-131">For information on IIS sub-application configuration, see <xref:host-and-deploy/iis/index#sub-applications>.</span></span>

### <a name="attributes-of-the-aspnetcore-element"></a><span data-ttu-id="75479-132">Atributy `aspNetCore` elementu</span><span class="sxs-lookup"><span data-stu-id="75479-132">Attributes of the `aspNetCore` element</span></span>

| <span data-ttu-id="75479-133">Atribut</span><span class="sxs-lookup"><span data-stu-id="75479-133">Attribute</span></span> | <span data-ttu-id="75479-134">Popis</span><span class="sxs-lookup"><span data-stu-id="75479-134">Description</span></span> | <span data-ttu-id="75479-135">Výchozí</span><span class="sxs-lookup"><span data-stu-id="75479-135">Default</span></span> |
| --------- | ----------- | :-----: |
| `arguments` | <p><span data-ttu-id="75479-136">Volitelný řetězcový atribut.</span><span class="sxs-lookup"><span data-stu-id="75479-136">Optional string attribute.</span></span></p><p><span data-ttu-id="75479-137">Argumenty ke spustitelnému souboru určenému v `processPath` .</span><span class="sxs-lookup"><span data-stu-id="75479-137">Arguments to the executable specified in `processPath`.</span></span></p> | |
| `disableStartUpErrorPage` | <p><span data-ttu-id="75479-138">Volitelný atribut typu Boolean.</span><span class="sxs-lookup"><span data-stu-id="75479-138">Optional Boolean attribute.</span></span></p><p><span data-ttu-id="75479-139">Pokud je nastaveno na true, stránka **502,5 – selhání procesu** se potlačí a stavová stránka 502 má nakonfigurovanou `web.config` prioritu.</span><span class="sxs-lookup"><span data-stu-id="75479-139">If true, the **502.5 - Process Failure** page is suppressed, and the 502 status code page configured in the `web.config` takes precedence.</span></span></p> | `false` |
| `forwardWindowsAuthToken` | <p><span data-ttu-id="75479-140">Volitelný atribut typu Boolean.</span><span class="sxs-lookup"><span data-stu-id="75479-140">Optional Boolean attribute.</span></span></p><p><span data-ttu-id="75479-141">Pokud je nastaveno na true, token se přepošle podřízenému procesu nasledujícímu `%ASPNETCORE_PORT%` jako záhlaví MS-ASPNETCORE-WINAUTHTOKEN na žádost.</span><span class="sxs-lookup"><span data-stu-id="75479-141">If true, the token is forwarded to the child process listening on `%ASPNETCORE_PORT%` as a header 'MS-ASPNETCORE-WINAUTHTOKEN' per request.</span></span> <span data-ttu-id="75479-142">Je zodpovědností za tento proces volání CloseHandle na tento token na žádost.</span><span class="sxs-lookup"><span data-stu-id="75479-142">It's the responsibility of that process to call CloseHandle on this token per request.</span></span></p> | `true` |
| `hostingModel` | <p><span data-ttu-id="75479-143">Volitelný řetězcový atribut.</span><span class="sxs-lookup"><span data-stu-id="75479-143">Optional string attribute.</span></span></p><p><span data-ttu-id="75479-144">Určuje model hostování jako vnitroprocesové ( `InProcess` / `inprocess` ) nebo mimo proces ( `OutOfProcess` / `outofprocess` ).</span><span class="sxs-lookup"><span data-stu-id="75479-144">Specifies the hosting model as in-process (`InProcess`/`inprocess`) or out-of-process (`OutOfProcess`/`outofprocess`).</span></span></p> | `InProcess`<br>`inprocess` |
| `processesPerApplication` | <p><span data-ttu-id="75479-145">Volitelný celočíselný atribut</span><span class="sxs-lookup"><span data-stu-id="75479-145">Optional integer attribute.</span></span></p><p><span data-ttu-id="75479-146">Určuje počet instancí procesu zadaných v `processPath` nastavení, které je možné vystavit pro jednotlivé aplikace.</span><span class="sxs-lookup"><span data-stu-id="75479-146">Specifies the number of instances of the process specified in the `processPath` setting that can be spun up per app.</span></span></p><p><span data-ttu-id="75479-147">&dagger;Pro hostování v rámci procesu je hodnota omezená na `1` .</span><span class="sxs-lookup"><span data-stu-id="75479-147">&dagger;For in-process hosting, the value is limited to `1`.</span></span></p><p><span data-ttu-id="75479-148">Nastavení `processesPerApplication` se nedoporučuje.</span><span class="sxs-lookup"><span data-stu-id="75479-148">Setting `processesPerApplication` is discouraged.</span></span> <span data-ttu-id="75479-149">Tento atribut bude v budoucí verzi odebrán.</span><span class="sxs-lookup"><span data-stu-id="75479-149">This attribute will be removed in a future release.</span></span></p> | <span data-ttu-id="75479-150">Výchozí `1`</span><span class="sxs-lookup"><span data-stu-id="75479-150">Default: `1`</span></span><br><span data-ttu-id="75479-151">Dlouhé `1`</span><span class="sxs-lookup"><span data-stu-id="75479-151">Min: `1`</span></span><br><span data-ttu-id="75479-152">Počet `100`&dagger;</span><span class="sxs-lookup"><span data-stu-id="75479-152">Max: `100`&dagger;</span></span> |
| `processPath` | <p><span data-ttu-id="75479-153">Povinný atribut řetězce.</span><span class="sxs-lookup"><span data-stu-id="75479-153">Required string attribute.</span></span></p><p><span data-ttu-id="75479-154">Cesta ke spustitelnému souboru, který spouští proces naslouchající na požadavky HTTP.</span><span class="sxs-lookup"><span data-stu-id="75479-154">Path to the executable that launches a process listening for HTTP requests.</span></span> <span data-ttu-id="75479-155">Jsou podporovány relativní cesty.</span><span class="sxs-lookup"><span data-stu-id="75479-155">Relative paths are supported.</span></span> <span data-ttu-id="75479-156">Pokud cesta začíná `.` , bude tato cesta považována za relativní vzhledem k kořenu webu.</span><span class="sxs-lookup"><span data-stu-id="75479-156">If the path begins with `.`, the path is considered to be relative to the site root.</span></span></p> | |
| `rapidFailsPerMinute` | <p><span data-ttu-id="75479-157">Volitelný celočíselný atribut</span><span class="sxs-lookup"><span data-stu-id="75479-157">Optional integer attribute.</span></span></p><p><span data-ttu-id="75479-158">Určuje počet pokusů, kolikrát `processPath` je povoleno selhání procesu určeného v rámci jedné minuty.</span><span class="sxs-lookup"><span data-stu-id="75479-158">Specifies the number of times the process specified in `processPath` is allowed to crash per minute.</span></span> <span data-ttu-id="75479-159">Pokud je tento limit překročen, modul ukončí spuštění procesu po zbytek minuty.</span><span class="sxs-lookup"><span data-stu-id="75479-159">If this limit is exceeded, the module stops launching the process for the remainder of the minute.</span></span></p><p><span data-ttu-id="75479-160">Nepodporováno v hostování v rámci procesu.</span><span class="sxs-lookup"><span data-stu-id="75479-160">Not supported with in-process hosting.</span></span></p> | <span data-ttu-id="75479-161">Výchozí `10`</span><span class="sxs-lookup"><span data-stu-id="75479-161">Default: `10`</span></span><br><span data-ttu-id="75479-162">Dlouhé `0`</span><span class="sxs-lookup"><span data-stu-id="75479-162">Min: `0`</span></span><br><span data-ttu-id="75479-163">Počet `100`</span><span class="sxs-lookup"><span data-stu-id="75479-163">Max: `100`</span></span> |
| `requestTimeout` | <p><span data-ttu-id="75479-164">Volitelný atribut TimeSpan.</span><span class="sxs-lookup"><span data-stu-id="75479-164">Optional timespan attribute.</span></span></p><p><span data-ttu-id="75479-165">Určuje dobu, po kterou modul ASP.NET Core čeká na odpověď od procesu, který naslouchá na% ASPNETCORE_PORT%.</span><span class="sxs-lookup"><span data-stu-id="75479-165">Specifies the duration for which the ASP.NET Core Module waits for a response from the process listening on %ASPNETCORE_PORT%.</span></span></p><p><span data-ttu-id="75479-166">Ve verzích modulu ASP.NET Core, které byly dodávány s vydáním ASP.NET Core 2,1 nebo vyšší, `requestTimeout` je určena v hodinách, minutách a sekundách.</span><span class="sxs-lookup"><span data-stu-id="75479-166">In versions of the ASP.NET Core Module that shipped with the release of ASP.NET Core 2.1 or later, the `requestTimeout` is specified in hours, minutes, and seconds.</span></span></p><p><span data-ttu-id="75479-167">Neplatí pro hostování v procesu.</span><span class="sxs-lookup"><span data-stu-id="75479-167">Doesn't apply to in-process hosting.</span></span> <span data-ttu-id="75479-168">Pro hostování v rámci procesu modul čeká, až aplikace zpracuje požadavek.</span><span class="sxs-lookup"><span data-stu-id="75479-168">For in-process hosting, the module waits for the app to process the request.</span></span></p><p><span data-ttu-id="75479-169">Platné hodnoty segmentů minut a sekund řetězce jsou v rozsahu 0-59.</span><span class="sxs-lookup"><span data-stu-id="75479-169">Valid values for minutes and seconds segments of the string are in the range 0-59.</span></span> <span data-ttu-id="75479-170">Použití `60` hodnoty v minutách nebo sekundách má za následek *500 – interní chyba serveru*.</span><span class="sxs-lookup"><span data-stu-id="75479-170">Use of `60` in the value for minutes or seconds results in a *500 - Internal Server Error*.</span></span></p> | <span data-ttu-id="75479-171">Výchozí `00:02:00`</span><span class="sxs-lookup"><span data-stu-id="75479-171">Default: `00:02:00`</span></span><br><span data-ttu-id="75479-172">Dlouhé `00:00:00`</span><span class="sxs-lookup"><span data-stu-id="75479-172">Min: `00:00:00`</span></span><br><span data-ttu-id="75479-173">Počet `360:00:00`</span><span class="sxs-lookup"><span data-stu-id="75479-173">Max: `360:00:00`</span></span> |
| `shutdownTimeLimit` | <p><span data-ttu-id="75479-174">Volitelný celočíselný atribut</span><span class="sxs-lookup"><span data-stu-id="75479-174">Optional integer attribute.</span></span></p><p><span data-ttu-id="75479-175">Doba v sekundách, po kterou modul čeká na řádné vypnutí spustitelného souboru, když `app_offline.htm` se zjistí soubor</span><span class="sxs-lookup"><span data-stu-id="75479-175">Duration in seconds that the module waits for the executable to gracefully shutdown when the `app_offline.htm` file is detected.</span></span></p> | <span data-ttu-id="75479-176">Výchozí `10`</span><span class="sxs-lookup"><span data-stu-id="75479-176">Default: `10`</span></span><br><span data-ttu-id="75479-177">Dlouhé `0`</span><span class="sxs-lookup"><span data-stu-id="75479-177">Min: `0`</span></span><br><span data-ttu-id="75479-178">Počet `600`</span><span class="sxs-lookup"><span data-stu-id="75479-178">Max: `600`</span></span> |
| `startupTimeLimit` | <p><span data-ttu-id="75479-179">Volitelný celočíselný atribut</span><span class="sxs-lookup"><span data-stu-id="75479-179">Optional integer attribute.</span></span></p><p><span data-ttu-id="75479-180">Doba v sekundách, kterou modul počká, než spustitelný soubor spustí proces, který na portu naslouchá.</span><span class="sxs-lookup"><span data-stu-id="75479-180">Duration in seconds that the module waits for the executable to start a process listening on the port.</span></span> <span data-ttu-id="75479-181">Pokud je tento časový limit překročen, modul proces ukončuje.</span><span class="sxs-lookup"><span data-stu-id="75479-181">If this time limit is exceeded, the module kills the process.</span></span></p><p><span data-ttu-id="75479-182">Při hostování *v procesu*: proces **není restartován a** **nepoužívá** `rapidFailsPerMinute` nastavení.</span><span class="sxs-lookup"><span data-stu-id="75479-182">When hosting *in-process*: The process is **not** restarted and does **not** use the `rapidFailsPerMinute` setting.</span></span></p><p><span data-ttu-id="75479-183">Při hostování *mimo proces*: modul se pokusí znovu spustit proces, když obdrží novou žádost, a pokračuje v pokusu o restartování procesu u dalších příchozích požadavků, pokud se aplikaci nepodaří spustit `rapidFailsPerMinute` několikrát během poslední minuty za minutu.</span><span class="sxs-lookup"><span data-stu-id="75479-183">When hosting *out-of-process*: The module attempts to relaunch the process when it receives a new request and continues to attempt to restart the process on subsequent incoming requests unless the app fails to start `rapidFailsPerMinute` number of times in the last rolling minute.</span></span></p><p><span data-ttu-id="75479-184">Hodnota 0 (nula **) není považována za** nekonečný časový limit.</span><span class="sxs-lookup"><span data-stu-id="75479-184">A value of 0 (zero) is **not** considered an infinite timeout.</span></span></p> | <span data-ttu-id="75479-185">Výchozí `120`</span><span class="sxs-lookup"><span data-stu-id="75479-185">Default: `120`</span></span><br><span data-ttu-id="75479-186">Dlouhé `0`</span><span class="sxs-lookup"><span data-stu-id="75479-186">Min: `0`</span></span><br><span data-ttu-id="75479-187">Počet `3600`</span><span class="sxs-lookup"><span data-stu-id="75479-187">Max: `3600`</span></span> |
| `stdoutLogEnabled` | <p><span data-ttu-id="75479-188">Volitelný atribut typu Boolean.</span><span class="sxs-lookup"><span data-stu-id="75479-188">Optional Boolean attribute.</span></span></p><p><span data-ttu-id="75479-189">Je-li nastavena hodnota true `stdout` a `stderr` pro proces určený v `processPath` je přesměrován do souboru určeného v `stdoutLogFile` .</span><span class="sxs-lookup"><span data-stu-id="75479-189">If true, `stdout` and `stderr` for the process specified in `processPath` are redirected to the file specified in `stdoutLogFile`.</span></span></p> | `false` |
| `stdoutLogFile` | <p><span data-ttu-id="75479-190">Volitelný řetězcový atribut.</span><span class="sxs-lookup"><span data-stu-id="75479-190">Optional string attribute.</span></span></p><p><span data-ttu-id="75479-191">Určuje relativní nebo absolutní cestu k souboru, pro který `stdout` a `stderr` z procesu určeného v části `processPath` jsou protokolovány.</span><span class="sxs-lookup"><span data-stu-id="75479-191">Specifies the relative or absolute file path for which `stdout` and `stderr` from the process specified in `processPath` are logged.</span></span> <span data-ttu-id="75479-192">Relativní cesty jsou relativní vzhledem k kořenu webu.</span><span class="sxs-lookup"><span data-stu-id="75479-192">Relative paths are relative to the root of the site.</span></span> <span data-ttu-id="75479-193">Každá cesta začínající řetězcem `.` je relativní vzhledem k kořenu webu a všechny ostatní cesty se považují za absolutní cesty.</span><span class="sxs-lookup"><span data-stu-id="75479-193">Any path starting with `.` are relative to the site root and all other paths are treated as absolute paths.</span></span> <span data-ttu-id="75479-194">Při vytvoření souboru protokolu se v modulu vytvoří všechny složky, které jsou v cestě zadané.</span><span class="sxs-lookup"><span data-stu-id="75479-194">Any folders provided in the path are created by the module when the log file is created.</span></span> <span data-ttu-id="75479-195">Pomocí oddělovačů podtržítka se `.log` do posledního segmentu cesty přidají časové razítko, ID procesu a Přípona souboru () `stdoutLogFile` .</span><span class="sxs-lookup"><span data-stu-id="75479-195">Using underscore delimiters, a timestamp, process ID, and file extension (`.log`) are added to the last segment of the `stdoutLogFile` path.</span></span> <span data-ttu-id="75479-196">Pokud `.\logs\stdout` je zadán jako hodnota, je ukázkový protokol stdout uložen jako `stdout_20180205194132_1934.log` ve `logs` složce při uložení na 2/5/2018 v 19:41:32 s ID procesu 1934.</span><span class="sxs-lookup"><span data-stu-id="75479-196">If `.\logs\stdout` is supplied as a value, an example stdout log is saved as `stdout_20180205194132_1934.log` in the `logs` folder when saved on 2/5/2018 at 19:41:32 with a process ID of 1934.</span></span></p> | `aspnetcore-stdout` |

### <a name="set-environment-variables"></a><span data-ttu-id="75479-197">Nastavení proměnných prostředí</span><span class="sxs-lookup"><span data-stu-id="75479-197">Set environment variables</span></span>

<span data-ttu-id="75479-198">Proměnné prostředí lze zadat pro proces v `processPath` atributu.</span><span class="sxs-lookup"><span data-stu-id="75479-198">Environment variables can be specified for the process in the `processPath` attribute.</span></span> <span data-ttu-id="75479-199">Zadejte proměnnou prostředí s `<environmentVariable>` podřízeným elementem `<environmentVariables>` elementu kolekce.</span><span class="sxs-lookup"><span data-stu-id="75479-199">Specify an environment variable with the `<environmentVariable>` child element of an `<environmentVariables>` collection element.</span></span> <span data-ttu-id="75479-200">Proměnné prostředí nastavené v této části mají přednost před proměnnými prostředí systému.</span><span class="sxs-lookup"><span data-stu-id="75479-200">Environment variables set in this section take precedence over system environment variables.</span></span>

<span data-ttu-id="75479-201">Následující příklad nastaví dvě proměnné prostředí v `web.config` .</span><span class="sxs-lookup"><span data-stu-id="75479-201">The following example sets two environment variables in `web.config`.</span></span> <span data-ttu-id="75479-202">`ASPNETCORE_ENVIRONMENT` nakonfiguruje prostředí aplikace na `Development` .</span><span class="sxs-lookup"><span data-stu-id="75479-202">`ASPNETCORE_ENVIRONMENT` configures the app's environment to `Development`.</span></span> <span data-ttu-id="75479-203">Vývojář může tuto hodnotu dočasně nastavit v souboru, aby se `web.config` vynutila načítání [stránky výjimky pro vývojáře](xref:fundamentals/error-handling) při ladění výjimky aplikace.</span><span class="sxs-lookup"><span data-stu-id="75479-203">A developer may temporarily set this value in the `web.config` file in order to force the [Developer Exception Page](xref:fundamentals/error-handling) to load when debugging an app exception.</span></span> <span data-ttu-id="75479-204">`CONFIG_DIR` je příkladem uživatelsky definované proměnné prostředí, kde vývojář napsal kód, který přečte hodnotu při spuštění, aby vytvořil cestu pro načtení konfiguračního souboru aplikace.</span><span class="sxs-lookup"><span data-stu-id="75479-204">`CONFIG_DIR` is an example of a user-defined environment variable, where the developer has written code that reads the value on startup to form a path for loading the app's configuration file.</span></span>

```xml
<aspNetCore processPath="dotnet"
      arguments=".\MyApp.dll"
      stdoutLogEnabled="false"
      stdoutLogFile=".\logs\stdout"
      hostingModel="inprocess">
  <environmentVariables>
    <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Development" />
    <environmentVariable name="CONFIG_DIR" value="f:\application_config" />
  </environmentVariables>
</aspNetCore>
```

> [!NOTE]
> <span data-ttu-id="75479-205">Alternativou k nastavení prostředí přímo v `web.config` je zahrnutí `<EnvironmentName>` vlastnosti do [publikačního profilu ( `.pubxml` )](xref:host-and-deploy/visual-studio-publish-profiles) nebo souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="75479-205">An alternative to setting the environment directly in `web.config` is to include the `<EnvironmentName>` property in the [publish profile (`.pubxml`)](xref:host-and-deploy/visual-studio-publish-profiles) or project file.</span></span> <span data-ttu-id="75479-206">Tento přístup nastaví prostředí v `web.config` okamžiku publikování projektu:</span><span class="sxs-lookup"><span data-stu-id="75479-206">This approach sets the environment in `web.config` when the project is published:</span></span>
>
> ```xml
> <PropertyGroup>
>   <EnvironmentName>Development</EnvironmentName>
> </PropertyGroup>
> ```

> [!WARNING]
> <span data-ttu-id="75479-207">Nastavte pouze `ASPNETCORE_ENVIRONMENT` proměnnou prostředí na `Development` pracovní a testovací servery, které nejsou přístupné nedůvěryhodným sítím, jako je například Internet.</span><span class="sxs-lookup"><span data-stu-id="75479-207">Only set the `ASPNETCORE_ENVIRONMENT` environment variable to `Development` on staging and testing servers that aren't accessible to untrusted networks, such as the Internet.</span></span>

## <a name="configuration-of-iis-with-webconfig"></a><span data-ttu-id="75479-208">Konfigurace služby IIS s `web.config`</span><span class="sxs-lookup"><span data-stu-id="75479-208">Configuration of IIS with `web.config`</span></span>

<span data-ttu-id="75479-209">Konfigurace služby IIS je ovlivněna `<system.webServer>` sekcí `web.config` pro scénáře služby IIS, které jsou funkční pro ASP.NET Core aplikací s modulem ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="75479-209">IIS configuration is influenced by the `<system.webServer>` section of `web.config` for IIS scenarios that are functional for ASP.NET Core apps with the ASP.NET Core Module.</span></span> <span data-ttu-id="75479-210">Například konfigurace služby IIS je funkční pro dynamickou kompresi.</span><span class="sxs-lookup"><span data-stu-id="75479-210">For example, IIS configuration is functional for dynamic compression.</span></span> <span data-ttu-id="75479-211">Pokud je služba IIS nakonfigurovaná na úrovni serveru tak, aby používala dynamickou kompresi, `<urlCompression>` může element v souboru aplikace tuto `web.config` aplikaci pro ASP.NET Core zakázat.</span><span class="sxs-lookup"><span data-stu-id="75479-211">If IIS is configured at the server level to use dynamic compression, the `<urlCompression>` element in the app's `web.config` file can disable it for an ASP.NET Core app.</span></span>

<span data-ttu-id="75479-212">Další informace najdete v následujících tématech:</span><span class="sxs-lookup"><span data-stu-id="75479-212">For more information, see the following topics:</span></span>

* [<span data-ttu-id="75479-213">Odkaz na konfiguraci pro `<system.webServer>`</span><span class="sxs-lookup"><span data-stu-id="75479-213">Configuration reference for `<system.webServer>`</span></span>](/iis/configuration/system.webServer/)
* <xref:host-and-deploy/aspnet-core-module>
* <xref:host-and-deploy/iis/modules>

<span data-ttu-id="75479-214">Nastavení proměnných prostředí pro jednotlivé aplikace spuštěné v izolovaných fondech aplikací (podporované pro IIS 10,0 nebo novější) najdete v části \* `AppCmd.exe` příkaz\* v tématu věnovaném oddílům [prostředí `<environmentVariables>` ](/iis/configuration/system.applicationHost/applicationPools/add/environmentVariables/#appcmdexe) v referenční dokumentaci ke službě IIS.</span><span class="sxs-lookup"><span data-stu-id="75479-214">To set environment variables for individual apps running in isolated app pools (supported for IIS 10.0 or later), see the *`AppCmd.exe` command* section of the [Environment Variables `<environmentVariables>`](/iis/configuration/system.applicationHost/applicationPools/add/environmentVariables/#appcmdexe) topic in the IIS reference documentation.</span></span>

## <a name="configuration-sections-of-webconfig"></a><span data-ttu-id="75479-215">Konfigurační oddíly pro `web.config`</span><span class="sxs-lookup"><span data-stu-id="75479-215">Configuration sections of `web.config`</span></span>

<span data-ttu-id="75479-216">Konfigurační oddíly aplikací ASP.NET 4. x `web.config` nepoužívá aplikace ASP.NET Core pro konfiguraci:</span><span class="sxs-lookup"><span data-stu-id="75479-216">Configuration sections of ASP.NET 4.x apps in `web.config` aren't used by ASP.NET Core apps for configuration:</span></span>

* `<system.web>`
* `<appSettings>`
* `<connectionStrings>`
* `<location>`

<span data-ttu-id="75479-217">Aplikace ASP.NET Core jsou nakonfigurovány pomocí jiných poskytovatelů konfigurace.</span><span class="sxs-lookup"><span data-stu-id="75479-217">ASP.NET Core apps are configured using other configuration providers.</span></span> <span data-ttu-id="75479-218">Další informace najdete v tématu [Konfigurace](xref:fundamentals/configuration/index).</span><span class="sxs-lookup"><span data-stu-id="75479-218">For more information, see [Configuration](xref:fundamentals/configuration/index).</span></span>

## <a name="transform-webconfig"></a><span data-ttu-id="75479-219">Transformace souboru web.config</span><span class="sxs-lookup"><span data-stu-id="75479-219">Transform web.config</span></span>

<span data-ttu-id="75479-220">Pokud potřebujete transformovat `web.config` při publikování, přečtěte si téma <xref:host-and-deploy/iis/transform-webconfig> .</span><span class="sxs-lookup"><span data-stu-id="75479-220">If you need to transform `web.config` on publish, see <xref:host-and-deploy/iis/transform-webconfig>.</span></span> <span data-ttu-id="75479-221">Možná budete muset transformovat `web.config` při publikování a nastavit proměnné prostředí na základě konfigurace, profilu nebo prostředí.</span><span class="sxs-lookup"><span data-stu-id="75479-221">You might need to transform `web.config` on publish to set environment variables based on the configuration, profile, or environment.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="75479-222">Další zdroje informací</span><span class="sxs-lookup"><span data-stu-id="75479-222">Additional resources</span></span>

* [<span data-ttu-id="75479-223">SLUŽBU \<system.webServer></span><span class="sxs-lookup"><span data-stu-id="75479-223">IIS \<system.webServer></span></span>](/iis/configuration/system.webServer/)
* <xref:host-and-deploy/iis/modules>
* <xref:host-and-deploy/iis/transform-webconfig>
