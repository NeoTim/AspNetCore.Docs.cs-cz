---
title: Migrace z ASP.NET Core 2,2 na 3,0 Preview
author: tdykstra
description: Přečtěte si, jak migrovat projekt ASP.NET Core 2,2 do ASP.NET Core 3,0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 08/05/2019
uid: migration/22-to-30
ms.openlocfilehash: 6312feb05cc917dd28e40b01f51a5fbf31c59e37
ms.sourcegitcommit: 2eb605f4f20ac4dd9de6c3b3e3453e108a357a21
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/06/2019
ms.locfileid: "68819882"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="a2822-103">Migrace z ASP.NET Core 2,2 na 3,0</span><span class="sxs-lookup"><span data-stu-id="a2822-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="a2822-104">[Scottem Addie](https://github.com/scottaddie) a [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="a2822-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="a2822-105">Tento článek vysvětluje, jak aktualizovat existující projekt ASP.NET Core 2,2 na ASP.NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="a2822-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a2822-106">Požadavky</span><span class="sxs-lookup"><span data-stu-id="a2822-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="a2822-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="a2822-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="a2822-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="a2822-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="a2822-109">Visual Studio pro Mac</span><span class="sxs-lookup"><span data-stu-id="a2822-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="a2822-110">Aktualizovat soubor projektu</span><span class="sxs-lookup"><span data-stu-id="a2822-110">Update the project file</span></span>

* <span data-ttu-id="a2822-111">Nastavte [moniker cílového rozhraní .NET Framework (TFM)](/dotnet/standard/frameworks) na `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="a2822-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="a2822-112">Odeberte všechny `<PackageReference>` z [Microsoft. AspNetCore. All](xref:fundamentals/metapackage) nebo [Microsoft. AspNetCore. app](xref:fundamentals/metapackage-app) Metapackage.</span><span class="sxs-lookup"><span data-stu-id="a2822-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="a2822-113">Odeberte všechny `<PackageReference>` balíčky [Microsoft. AspNetCore. Razor. Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) .</span><span class="sxs-lookup"><span data-stu-id="a2822-113">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package.</span></span>

* <span data-ttu-id="a2822-114">Aktualizuje atribut u zbývajících `<PackageReference>` prvků pro `Microsoft.AspNetCore.*` balíčky `3.0.0-preview5-19227-01`na aktuální verzi Preview (například). `Version`</span><span class="sxs-lookup"><span data-stu-id="a2822-114">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="a2822-115">Pokud není k dispozici žádná verze 3,0 balíčku, balíček může být zastaralý v 3,0.</span><span class="sxs-lookup"><span data-stu-id="a2822-115">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="a2822-116">Mnohé z těchto balíčků jsou součástí `Microsoft.AspNetCore.App` a neměly by být odkazovány jednotlivě.</span><span class="sxs-lookup"><span data-stu-id="a2822-116">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="a2822-117">Předběžný seznam balíčků, které už nejsou v 3,0, najdete v tématu [zastavení výroby balíčků pro sestavení sdílených rozhraní v 3,0 (ASPNET/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="a2822-117">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="a2822-118">*Sdílené rozhraní* je sada sestavení (soubory *. dll* ), která jsou nainstalována na počítači `Microsoft.AspNetCore.App`a na kterou odkazuje.</span><span class="sxs-lookup"><span data-stu-id="a2822-118">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="a2822-119">Další informace najdete v tématu [sdílené rozhraní](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="a2822-119">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="a2822-120">Sestavení pro několik významných součástí byla odebrána z `Microsoft.AspNetCore.App` v 3,0.</span><span class="sxs-lookup"><span data-stu-id="a2822-120">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="a2822-121">Přidejte `<PackageReference>` prvky, pokud používáte rozhraní API z balíčků uvedených v [sestaveních odebraných z Microsoft. AspNetCore. app 3,0 (ASPNET/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="a2822-121">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="a2822-122">Mezi příklady odebraných součástí patří:</span><span class="sxs-lookup"><span data-stu-id="a2822-122">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="a2822-123">Seznam sestavení, která jsou expedována v `Microsoft.AspNetCore.App` , nebyl finalizován a bude změněn před 3,0 RTM.</span><span class="sxs-lookup"><span data-stu-id="a2822-123">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="a2822-124">Vezměte v úvahu následující kód:</span><span class="sxs-lookup"><span data-stu-id="a2822-124">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="a2822-125">Metoda volaná v předchozím kódu je obsažena v `Microsoft.AspNet.WebApi.Client`. `ReadAsAsync`</span><span class="sxs-lookup"><span data-stu-id="a2822-125">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="a2822-126">Nainstalujte balíček NuGet [Microsoft. ASPNET. WebApi. Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) , abyste vyřešili problém s kompilací v 3,0.</span><span class="sxs-lookup"><span data-stu-id="a2822-126">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="a2822-127">Přidejte [podporu JSON.NET](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="a2822-127">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="a2822-128">Projekty jsou ve výchozím nastavení pro [model hostování v rámci procesu](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) v ASP.NET Core 3,0 nebo novějším.</span><span class="sxs-lookup"><span data-stu-id="a2822-128">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="a2822-129">Případně můžete odebrat `<AspNetCoreHostingModel>` vlastnost v souboru projektu, pokud je `InProcess`její hodnota.</span><span class="sxs-lookup"><span data-stu-id="a2822-129">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="a2822-130">Podpora Json.NET</span><span class="sxs-lookup"><span data-stu-id="a2822-130">Json.NET support</span></span>

<span data-ttu-id="a2822-131">Jako součást práce pro [zlepšení ASP.NET Core sdílené architektury](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)se [JSON.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) z ASP.NET Core sdílené architektury odebral.</span><span class="sxs-lookup"><span data-stu-id="a2822-131">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="a2822-132">Použití Json.NET v projektu ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="a2822-132">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="a2822-133">Přidejte odkaz na balíček [Microsoft. AspNetCore. Mvc. NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="a2822-133">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="a2822-134">Aktualizujte `Startup.ConfigureServices` volání `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="a2822-134">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="a2822-135">`AddNewtonsoftJson`je kompatibilní s novými metodami registrace služby MVC:</span><span class="sxs-lookup"><span data-stu-id="a2822-135">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="a2822-136">Nastavení Json.NET lze nastavit v volání `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="a2822-136">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="a2822-137">Registrace služby MVC</span><span class="sxs-lookup"><span data-stu-id="a2822-137">MVC service registration</span></span>

<span data-ttu-id="a2822-138">ASP.NET Core 3,0 přidává nové možnosti pro registraci scénářů MVC `Startup.ConfigureServices`v rámci.</span><span class="sxs-lookup"><span data-stu-id="a2822-138">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="a2822-139">K dispozici jsou tři nové metody rozšíření na `IServiceCollection` nejvyšší úrovni související s scénáři MVC.</span><span class="sxs-lookup"><span data-stu-id="a2822-139">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="a2822-140">Šablony používají tyto nové metody místo `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="a2822-140">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="a2822-141">Nicméně se `AddMvc` i nadále chová jako v předchozích verzích.</span><span class="sxs-lookup"><span data-stu-id="a2822-141">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="a2822-142">Následující příklad přidá podporu pro řadiče a funkce související s rozhraním API, ale ne zobrazení nebo stránky.</span><span class="sxs-lookup"><span data-stu-id="a2822-142">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="a2822-143">Šablona rozhraní API používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="a2822-143">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="a2822-144">Následující příklad přidá podporu pro řadiče, funkce související s rozhraním API a zobrazení, ale ne stránky.</span><span class="sxs-lookup"><span data-stu-id="a2822-144">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="a2822-145">Šablona webové aplikace (MVC) používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="a2822-145">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="a2822-146">V následujícím příkladu se přidá podpora pro Razor Pages a minimální Podpora řadičů.</span><span class="sxs-lookup"><span data-stu-id="a2822-146">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="a2822-147">Šablona webové aplikace používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="a2822-147">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="a2822-148">Nové metody lze také kombinovat.</span><span class="sxs-lookup"><span data-stu-id="a2822-148">The new methods can also be combined.</span></span> <span data-ttu-id="a2822-149">Následující příklad je ekvivalentní volání `AddMvc` v ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="a2822-149">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="a2822-150">Aktualizovat spouštěcí kód směrování</span><span class="sxs-lookup"><span data-stu-id="a2822-150">Update routing startup code</span></span>

<span data-ttu-id="a2822-151">Pokud je to aplikace `UseMvc` , `UseSignalR`nebo pokud je to možné, proveďte migraci aplikace do [Směrování koncových bodů](xref:fundamentals/routing) .</span><span class="sxs-lookup"><span data-stu-id="a2822-151">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="a2822-152">Pro zlepšení kompatibility směrování koncových bodů s předchozími verzemi MVC jsme vrátili některé změny v adrese URL představené v ASP.NET Core 2,2.</span><span class="sxs-lookup"><span data-stu-id="a2822-152">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="a2822-153">Pokud jste narazili na problémy s používáním směrování koncových bodů v 2,2, můžete očekávat vylepšení ASP.NET Core 3,0 s následujícími výjimkami:</span><span class="sxs-lookup"><span data-stu-id="a2822-153">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="a2822-154">Pokud aplikace implementuje `IRouter` nebo dědí z `Route`, možná se budete chtít vyhnout migraci v tuto chvíli.</span><span class="sxs-lookup"><span data-stu-id="a2822-154">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a2822-155">Poskytněte nám svůj názor na [plán pro migraci implementací na základě IRouter do směrování koncových bodů](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="a2822-155">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="a2822-156">Pokud aplikace přímo přistupuje k `RouteData.Routers` ovládacímu prvku MVC, možná se nebudete chtít v tomto okamžiku migrovat.</span><span class="sxs-lookup"><span data-stu-id="a2822-156">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a2822-157">Poskytněte nám svůj názor na [pokyny k migraci pro použití parametr RouteData. routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="a2822-157">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="a2822-158">Směrování koncového bodu podporuje stejnou syntaxi vzorů směrování a funkce vytváření vzorů směrování jako `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="a2822-158">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="a2822-159">Směrování koncového `IRouteContraint`bodu podporuje.</span><span class="sxs-lookup"><span data-stu-id="a2822-159">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="a2822-160">Směrování koncového `[Route]`bodu `[HttpGet]`podporuje, a další atributy směrování MVC.</span><span class="sxs-lookup"><span data-stu-id="a2822-160">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="a2822-161">Pro většinu aplikací vyžaduje pouze `Startup` změny.</span><span class="sxs-lookup"><span data-stu-id="a2822-161">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="a2822-162">Migrovat Startup. Configure</span><span class="sxs-lookup"><span data-stu-id="a2822-162">Migrate Startup.Configure</span></span>

<span data-ttu-id="a2822-163">Obecné pokyny:</span><span class="sxs-lookup"><span data-stu-id="a2822-163">General advice:</span></span>

* <span data-ttu-id="a2822-164">Přidat `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a2822-164">Add `UseRouting`.</span></span> 
* <span data-ttu-id="a2822-165">Pokud je aplikace volána `UseStaticFiles`, umístěte `UseStaticFiles` **před** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a2822-165">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="a2822-166">Pokud aplikace používá funkce `AuthorizePage` ověřování/autorizace `UseAuthentication` , jako je nebo `[Authorize]`, umístěte volání a `UseAuthorization` **After** `UseRouting` (a **po** `UseCors` použití middlewaru CORS).</span><span class="sxs-lookup"><span data-stu-id="a2822-166">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting` (and **after** `UseCors` if CORS Middleware is used).</span></span>
* <span data-ttu-id="a2822-167">`UseMvc` Nahraďte `UseSignalR` nebo s `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a2822-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>
* <span data-ttu-id="a2822-168">Pokud aplikace používá scénáře [CORS](xref:security/cors) , například `[EnableCors]`, umístěte volání do `UseAuthentication` `UseCors` jakýchkoli jiných middlewarů, které používají CORS (například na místo `UseCors` před, `UseAuthorization`a `UseMvc`).</span><span class="sxs-lookup"><span data-stu-id="a2822-168">If the app uses [CORS](xref:security/cors) scenarios, such as `[EnableCors]`, place the call to `UseCors` before any other middlewares that use CORS (for example, place `UseCors` before `UseAuthentication`, `UseAuthorization`, and `UseMvc`).</span></span>

<span data-ttu-id="a2822-169">Následuje příklad `Startup.Configure` typické aplikace ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="a2822-169">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="a2822-170">Po aktualizaci předchozího `Startup.Configure` kódu:</span><span class="sxs-lookup"><span data-stu-id="a2822-170">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="a2822-171">Doprovodné materiály k zabezpečení middlewaru</span><span class="sxs-lookup"><span data-stu-id="a2822-171">Security middleware guidance</span></span>

<span data-ttu-id="a2822-172">Podpora pro autorizaci a CORS je sjednocená [](xref:fundamentals/middleware/index) v rámci přístupu middlewaru.</span><span class="sxs-lookup"><span data-stu-id="a2822-172">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="a2822-173">To umožňuje používat stejný middlewar a funkčnost v těchto scénářích.</span><span class="sxs-lookup"><span data-stu-id="a2822-173">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="a2822-174">V této verzi je k dispozici aktualizovaný middleware autorizace a middleware CORS je vylepšený, aby bylo možné pochopit atributy používané řadiči MVC.</span><span class="sxs-lookup"><span data-stu-id="a2822-174">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="a2822-175">CORS</span><span class="sxs-lookup"><span data-stu-id="a2822-175">CORS</span></span>

<span data-ttu-id="a2822-176">V minulosti mohlo být obtížné nakonfigurovat CORS.</span><span class="sxs-lookup"><span data-stu-id="a2822-176">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="a2822-177">Middleware byl poskytnut pro použití v některých případech použití, ale filtry MVC byly určeny k použití **bez** middlewaru v jiných případech použití.</span><span class="sxs-lookup"><span data-stu-id="a2822-177">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="a2822-178">U ASP.NET Core 3,0 doporučujeme, aby všechny aplikace, které vyžadují CORS, používaly middleware CORS v kombinaci s směrováním koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="a2822-178">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="a2822-179">`UseCors`dá se zadat s výchozí zásadou a `[EnableCors]` `[DisableCors]` pomocí atributů můžete v případě potřeby přepsat výchozí zásady.</span><span class="sxs-lookup"><span data-stu-id="a2822-179">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="a2822-180">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="a2822-180">In the following example:</span></span>

* <span data-ttu-id="a2822-181">U všech koncových bodů s `default` pojmenovanou zásadou je povolená CORS.</span><span class="sxs-lookup"><span data-stu-id="a2822-181">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="a2822-182">Třída zakáže CORS `[DisableCors]` s atributem. `MyController`</span><span class="sxs-lookup"><span data-stu-id="a2822-182">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="a2822-183">Autorizace</span><span class="sxs-lookup"><span data-stu-id="a2822-183">Authorization</span></span>

<span data-ttu-id="a2822-184">V dřívějších verzích ASP.NET Core byla poskytnuta podpora autorizace prostřednictvím `[Authorize]` atributu.</span><span class="sxs-lookup"><span data-stu-id="a2822-184">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="a2822-185">Autorizační middleware není k dispozici.</span><span class="sxs-lookup"><span data-stu-id="a2822-185">Authorization middleware wasn't available.</span></span> <span data-ttu-id="a2822-186">V ASP.NET Core 3,0 se vyžaduje middleware autorizace.</span><span class="sxs-lookup"><span data-stu-id="a2822-186">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="a2822-187">Doporučujeme umístit middleware autorizace ASP.NET Core (`UseAuthorization`) hned po. `UseAuthentication`</span><span class="sxs-lookup"><span data-stu-id="a2822-187">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="a2822-188">Middleware autorizace se dá nakonfigurovat taky s výchozími zásadami, které se dají přepsat.</span><span class="sxs-lookup"><span data-stu-id="a2822-188">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="a2822-189">V ASP.NET Core 3,0 nebo novějším `UseAuthorization` se volá v `Startup.Configure`a následující `HomeController` vyžaduje přihlášeného uživatele:</span><span class="sxs-lookup"><span data-stu-id="a2822-189">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="a2822-190">Pokud aplikace používá `AuthorizeFilter` jako globální filtr v MVC, doporučujeme, abyste kód refaktoringu poskytli zásadám `UseAuthorization` pro middleware.</span><span class="sxs-lookup"><span data-stu-id="a2822-190">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="a2822-191">V následujícím příkladu se vlastní zásady použijí na všechny požadavky, když `UseAuthorization` se zavolá, `HomeController` a umožňuje přístup bez přihlašování uživatele do aplikace:</span><span class="sxs-lookup"><span data-stu-id="a2822-191">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="a2822-192">Ověřování pomocí middlewaru funguje bez rozhraní, které má konkrétní znalosti o autorizaci.</span><span class="sxs-lookup"><span data-stu-id="a2822-192">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="a2822-193">[Kontroly stavu](xref:host-and-deploy/health-checks) například nemají žádné konkrétní znalosti o autorizaci, ale kontroly stavu můžou mít konfigurovatelné zásady autorizace, které používá middleware.</span><span class="sxs-lookup"><span data-stu-id="a2822-193">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="a2822-194">V následujícím příkladu `UseAuthorization` zpracovává autorizaci bez výchozích zásad, `/healthz` ale koncový bod kontroly stavu vyžaduje, aby `admin` uživatel byl v roli:</span><span class="sxs-lookup"><span data-stu-id="a2822-194">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="a2822-195">Ochrana je implementovaná pro některé scénáře.</span><span class="sxs-lookup"><span data-stu-id="a2822-195">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="a2822-196">`UseEndpoint`middleware vyvolá výjimku, pokud je zásada autorizace nebo CORS vynechána z důvodu chybějícího middlewaru.</span><span class="sxs-lookup"><span data-stu-id="a2822-196">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="a2822-197">Podpora analyzátoru, která poskytuje další názory na neplatnou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="a2822-197">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="a2822-198">Migrace signálu</span><span class="sxs-lookup"><span data-stu-id="a2822-198">Migrate SignalR</span></span>

<span data-ttu-id="a2822-199">Mapování rozbočovačů signálu se teď provádí v rámci `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a2822-199">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a2822-200">Namapujte každé centrum `MapHub`pomocí.</span><span class="sxs-lookup"><span data-stu-id="a2822-200">Map each hub with `MapHub`.</span></span> <span data-ttu-id="a2822-201">Stejně jako v předchozích verzích je každé centrum výslovně uvedené.</span><span class="sxs-lookup"><span data-stu-id="a2822-201">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="a2822-202">V následujícím příkladu se přidá podpora pro `ChatHub` centrum signalizace:</span><span class="sxs-lookup"><span data-stu-id="a2822-202">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="a2822-203">Migrace řadičů MVC</span><span class="sxs-lookup"><span data-stu-id="a2822-203">Migrate MVC controllers</span></span>

<span data-ttu-id="a2822-204">Mapování řadičů se teď provádí v rámci `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a2822-204">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a2822-205">Přidejte `MapControllers` , pokud aplikace používá směrování atributů.</span><span class="sxs-lookup"><span data-stu-id="a2822-205">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="a2822-206">Vzhledem k tomu, že směrování zahrnuje podporu pro mnoho platforem v ASP.NET Core 3,0 nebo novějších, je přidání řadičů směrovaných k atributům výslovný souhlas.</span><span class="sxs-lookup"><span data-stu-id="a2822-206">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="a2822-207">Nahraďte následující:</span><span class="sxs-lookup"><span data-stu-id="a2822-207">Replace the following:</span></span>

* <span data-ttu-id="a2822-208">`MapRoute`řetězce`MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a2822-208">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="a2822-209">`MapAreaRoute`řetězce`MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a2822-209">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="a2822-210">Vzhledem k tomu, že směrování nyní zahrnuje podporu pro více než jen MVC, se terminologie změnila, aby tyto metody jasně zajistila, co dělají.</span><span class="sxs-lookup"><span data-stu-id="a2822-210">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="a2822-211">Konvenční trasy, například `MapControllerRoute` / `MapAreaControllerRoute` ,jsouaplikoványvpořadí,vjakémbylypřidány.`MapDefaultControllerRoute` /</span><span class="sxs-lookup"><span data-stu-id="a2822-211">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="a2822-212">Nejprve umístěte konkrétnější trasy (například trasy pro oblast).</span><span class="sxs-lookup"><span data-stu-id="a2822-212">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="a2822-213">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="a2822-213">In the following example:</span></span>

* <span data-ttu-id="a2822-214">`MapControllers`Přidá podporu pro řadiče směrované atributy.</span><span class="sxs-lookup"><span data-stu-id="a2822-214">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="a2822-215">`MapAreaControllerRoute`Přidá konvenční trasu pro řadiče v oblasti.</span><span class="sxs-lookup"><span data-stu-id="a2822-215">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="a2822-216">`MapControllerRoute`Přidá konvenční trasu pro řadiče.</span><span class="sxs-lookup"><span data-stu-id="a2822-216">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="a2822-217">Migrace Razor Pages</span><span class="sxs-lookup"><span data-stu-id="a2822-217">Migrate Razor Pages</span></span>

<span data-ttu-id="a2822-218">Mapování Razor Pages nyní probíhá uvnitř `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a2822-218">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="a2822-219">Přidejte `MapRazorPages` , pokud aplikace používá Razor Pages.</span><span class="sxs-lookup"><span data-stu-id="a2822-219">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="a2822-220">Vzhledem k tomu, že směrování koncových bodů zahrnuje podporu pro spoustu platforem, přidání Razor Pages je nyní výslovným souhlasem.</span><span class="sxs-lookup"><span data-stu-id="a2822-220">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="a2822-221">V následujícím příkladu `MapRazorPages` přidá podporu pro Razor Pages:</span><span class="sxs-lookup"><span data-stu-id="a2822-221">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="a2822-222">Použití MVC bez směrování koncových bodů</span><span class="sxs-lookup"><span data-stu-id="a2822-222">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="a2822-223">Použití MVC prostřednictvím `UseMvc` nebo `UseMvcWithDefaultRoute` v ASP.NET Core 3,0 vyžaduje explicitní výslovný souhlas v rámci `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="a2822-223">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="a2822-224">To je nutné, protože MVC musí zjistit, jestli se může při inicializaci spoléhat na autorizaci a middleware CORS.</span><span class="sxs-lookup"><span data-stu-id="a2822-224">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="a2822-225">K dispozici je analyzátor, který se upozorní, pokud se aplikace pokusí použít nepodporovanou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="a2822-225">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="a2822-226">Pokud aplikace vyžaduje starší `IRouter` verzi podpory, zakažte `EnableEndpointRouting` použití některého z následujících přístupů v `Startup.ConfigureServices`nástroji:</span><span class="sxs-lookup"><span data-stu-id="a2822-226">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="a2822-227">Migrace kontrol stavu</span><span class="sxs-lookup"><span data-stu-id="a2822-227">Migrate health checks</span></span>

<span data-ttu-id="a2822-228">Kontroly stavu lze použít jako *směrovač* s směrováním koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="a2822-228">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="a2822-229">Přidejte `MapHealthChecks` k použití kontrol stavu pomocí směrování koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="a2822-229">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="a2822-230">Metoda přijímá argumenty `UseHealthChecks`podobné. `MapHealthChecks`</span><span class="sxs-lookup"><span data-stu-id="a2822-230">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="a2822-231">Výhodou použití `MapHealthChecks` funkce Over `UseHealthChecks` je možnost použít autorizaci a mít větší jemně odstupňovanou kontrolu nad zásadami pro porovnání.</span><span class="sxs-lookup"><span data-stu-id="a2822-231">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="a2822-232">V následujícím příkladu `MapHealthChecks` je volána pro koncový bod kontroly stavu v `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="a2822-232">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="a2822-233">HostBuilder nahrazuje WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="a2822-233">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="a2822-234">Šablony ASP.NET Core 3,0 používají [obecného hostitele](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="a2822-234">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="a2822-235">Předchozí verze používaly [webového hostitele](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="a2822-235">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="a2822-236">Následující kód ukazuje třídu vygenerovanou `Program` šablonou ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="a2822-236">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="a2822-237">Následující kód ukazuje třídu vygenerovanou `Program` šablonou ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="a2822-237">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="a2822-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>zůstává v 3,0 a je typ `webBuilder` zobrazený v předchozí ukázce kódu.</span><span class="sxs-lookup"><span data-stu-id="a2822-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="a2822-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>bude zastaralá v budoucí verzi a nahrazena nástrojem `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="a2822-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="a2822-240">Nejvýznamnější změna z `WebHostBuilder` na `HostBuilder` je v [vkládání závislostí (di)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="a2822-240">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="a2822-241">Při použití `HostBuilder`nástroje lze <xref:Microsoft.Extensions.Configuration.IConfiguration> <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> vložit`Startup`pouze konstruktor.</span><span class="sxs-lookup"><span data-stu-id="a2822-241">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="a2822-242">Omezení `HostBuilder` di:</span><span class="sxs-lookup"><span data-stu-id="a2822-242">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="a2822-243">Povolí sestavení kontejneru DI jenom jednou.</span><span class="sxs-lookup"><span data-stu-id="a2822-243">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="a2822-244">Předejde výsledným problémům životního cyklu objektů, jako je například řešení více instancí singleton.</span><span class="sxs-lookup"><span data-stu-id="a2822-244">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="a2822-245">Aktualizovat kód signálu</span><span class="sxs-lookup"><span data-stu-id="a2822-245">Update SignalR code</span></span>

<span data-ttu-id="a2822-246">`System.Text.Json`je teď výchozím protokolem rozbočovače používaným klientem i serverem.</span><span class="sxs-lookup"><span data-stu-id="a2822-246">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="a2822-247">V `Startup.ConfigureServices`nástroji zavolejte `AddJsonProtocol` na nastavit možnosti serializátoru.</span><span class="sxs-lookup"><span data-stu-id="a2822-247">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="a2822-248">**WebServer**</span><span class="sxs-lookup"><span data-stu-id="a2822-248">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="a2822-249">**Služba**</span><span class="sxs-lookup"><span data-stu-id="a2822-249">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="a2822-250">Přepnout na Newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="a2822-250">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="a2822-251">Pokud používáte funkce `Newtonsoft.Json` nástroje, které nejsou podporované v `System.Text.Json`, můžete přejít zpátky na `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="a2822-251">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="a2822-252">Nainstalujte balíček NuGet [Microsoft. AspNetCore. signaler. Protocols. NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) .</span><span class="sxs-lookup"><span data-stu-id="a2822-252">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="a2822-253">Na straně klienta řetězit `AddNewtonsoftJsonProtocol` volání metody `HubConnectionBuilder` do instance:</span><span class="sxs-lookup"><span data-stu-id="a2822-253">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="a2822-254">Na serveru řetězit `AddNewtonsoftJsonProtocol` volání `AddSignalR` metody volání `Startup.ConfigureServices`metody:</span><span class="sxs-lookup"><span data-stu-id="a2822-254">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="a2822-255">Přihlásit se ke kompilaci za běhu</span><span class="sxs-lookup"><span data-stu-id="a2822-255">Opt in to runtime compilation</span></span>

<span data-ttu-id="a2822-256">V 3,0 je kompilace za běhu v případě výslovného souhlasu.</span><span class="sxs-lookup"><span data-stu-id="a2822-256">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="a2822-257">Chcete-li povolit kompilaci za https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation běhu, přečtěte si téma.</span><span class="sxs-lookup"><span data-stu-id="a2822-257">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
