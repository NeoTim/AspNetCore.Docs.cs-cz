---
title: Migrace z ASP.NET Core 2,2 na 3,0 Preview
author: rick-anderson
description: Přečtěte si, jak migrovat projekt ASP.NET Core 2,2 do ASP.NET Core 3,0.
ms.author: riande
ms.custom: mvc
ms.date: 09/03/2019
uid: migration/22-to-30
ms.openlocfilehash: 97472d6fd467cd471590106d271acd448c852468
ms.sourcegitcommit: 8b36f75b8931ae3f656e2a8e63572080adc78513
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/05/2019
ms.locfileid: "70310490"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="0377b-103">Migrace z ASP.NET Core 2,2 na 3,0</span><span class="sxs-lookup"><span data-stu-id="0377b-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="0377b-104">[Scottem Addie](https://github.com/scottaddie) a [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="0377b-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="0377b-105">Tento článek vysvětluje, jak aktualizovat existující projekt ASP.NET Core 2,2 na ASP.NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="0377b-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="0377b-106">Požadavky</span><span class="sxs-lookup"><span data-stu-id="0377b-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="0377b-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="0377b-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="0377b-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="0377b-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="0377b-109">Visual Studio pro Mac</span><span class="sxs-lookup"><span data-stu-id="0377b-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="0377b-110">Aktualizovat soubor projektu</span><span class="sxs-lookup"><span data-stu-id="0377b-110">Update the project file</span></span>

* <span data-ttu-id="0377b-111">Nastavte [moniker cílového rozhraní .NET Framework (TFM)](/dotnet/standard/frameworks) na `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="0377b-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="0377b-112">Odeberte všechny `<PackageReference>` z [Microsoft. AspNetCore. All](xref:fundamentals/metapackage) nebo [Microsoft. AspNetCore. app](xref:fundamentals/metapackage-app) Metapackage.</span><span class="sxs-lookup"><span data-stu-id="0377b-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="0377b-113">Odeberte všechny `<PackageReference>` balíčky [Microsoft. AspNetCore. Razor. Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) .</span><span class="sxs-lookup"><span data-stu-id="0377b-113">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package.</span></span>

* <span data-ttu-id="0377b-114">Pokud vaše aplikace používá [analyzátory rozhraní API](xref:web-api/advanced/analyzers), odeberte `<PackageReference>` všechny prvky pro balíček [Microsoft. AspNetCore. Mvc. API. Analyzer](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Api.Analyzers/) .</span><span class="sxs-lookup"><span data-stu-id="0377b-114">If your application uses [API analyzers](xref:web-api/advanced/analyzers), remove any `<PackageReference>` element for the [Microsoft.AspNetCore.Mvc.Api.Analyzers](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Api.Analyzers/) package.</span></span> <span data-ttu-id="0377b-115">Upravte soubor projektu tak, aby používal analyzátor dodaný jako součást .NET Core SDK:</span><span class="sxs-lookup"><span data-stu-id="0377b-115">Edit your project file to use the analyzer shipped as part of the .NET Core SDK:</span></span>

```xml
<PropertyGroup>
 <IncludeOpenAPIAnalyzers>true</IncludeOpenAPIAnalyzers>
</PropertyGroup>
```

* <span data-ttu-id="0377b-116">Aktualizuje atribut u zbývajících `<PackageReference>` prvků pro `Microsoft.AspNetCore.*` balíčky `3.0.0-preview5-19227-01`na aktuální verzi Preview (například). `Version`</span><span class="sxs-lookup"><span data-stu-id="0377b-116">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="0377b-117">Pokud není k dispozici žádná verze 3,0 balíčku, balíček může být zastaralý v 3,0.</span><span class="sxs-lookup"><span data-stu-id="0377b-117">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="0377b-118">Mnohé z těchto balíčků jsou součástí `Microsoft.AspNetCore.App` a neměly by být odkazovány jednotlivě.</span><span class="sxs-lookup"><span data-stu-id="0377b-118">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="0377b-119">Předběžný seznam balíčků, které už nejsou v 3,0, najdete v tématu [zastavení výroby balíčků pro sestavení sdílených rozhraní v 3,0 (ASPNET/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="0377b-119">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="0377b-120">*Sdílené rozhraní* je sada sestavení (soubory *. dll* ), která jsou nainstalována na počítači `Microsoft.AspNetCore.App`a na kterou odkazuje.</span><span class="sxs-lookup"><span data-stu-id="0377b-120">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="0377b-121">Další informace najdete v tématu [sdílené rozhraní](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="0377b-121">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="0377b-122">Sestavení pro několik významných součástí byla odebrána z `Microsoft.AspNetCore.App` v 3,0.</span><span class="sxs-lookup"><span data-stu-id="0377b-122">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="0377b-123">Přidejte `<PackageReference>` prvky, pokud používáte rozhraní API z balíčků uvedených v [sestaveních odebraných z Microsoft. AspNetCore. app 3,0 (ASPNET/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="0377b-123">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="0377b-124">Mezi příklady odebraných součástí patří:</span><span class="sxs-lookup"><span data-stu-id="0377b-124">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="0377b-125">Seznam sestavení, která jsou expedována v `Microsoft.AspNetCore.App` , nebyl finalizován a bude změněn před 3,0 RTM.</span><span class="sxs-lookup"><span data-stu-id="0377b-125">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="0377b-126">Vezměte v úvahu následující kód:</span><span class="sxs-lookup"><span data-stu-id="0377b-126">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="0377b-127">Metoda volaná v předchozím kódu je obsažena v `Microsoft.AspNet.WebApi.Client`. `ReadAsAsync`</span><span class="sxs-lookup"><span data-stu-id="0377b-127">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="0377b-128">Nainstalujte balíček NuGet [Microsoft. ASPNET. WebApi. Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) , abyste vyřešili problém s kompilací v 3,0.</span><span class="sxs-lookup"><span data-stu-id="0377b-128">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="0377b-129">Přidejte [podporu JSON.NET](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="0377b-129">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="0377b-130">Projekty jsou ve výchozím nastavení pro [model hostování v rámci procesu](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) v ASP.NET Core 3,0 nebo novějším.</span><span class="sxs-lookup"><span data-stu-id="0377b-130">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="0377b-131">Případně můžete odebrat `<AspNetCoreHostingModel>` vlastnost v souboru projektu, pokud je `InProcess`její hodnota.</span><span class="sxs-lookup"><span data-stu-id="0377b-131">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="0377b-132">Podpora Json.NET</span><span class="sxs-lookup"><span data-stu-id="0377b-132">Json.NET support</span></span>

<span data-ttu-id="0377b-133">Jako součást práce pro [zlepšení ASP.NET Core sdílené architektury](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)se [JSON.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) z ASP.NET Core sdílené architektury odebral.</span><span class="sxs-lookup"><span data-stu-id="0377b-133">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="0377b-134">Použití Json.NET v projektu ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="0377b-134">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="0377b-135">Přidejte odkaz na balíček [Microsoft. AspNetCore. Mvc. NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="0377b-135">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="0377b-136">Aktualizujte `Startup.ConfigureServices` volání `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="0377b-136">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="0377b-137">`AddNewtonsoftJson`je kompatibilní s novými metodami registrace služby MVC:</span><span class="sxs-lookup"><span data-stu-id="0377b-137">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="0377b-138">Nastavení Json.NET lze nastavit v volání `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="0377b-138">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="0377b-139">Registrace služby MVC</span><span class="sxs-lookup"><span data-stu-id="0377b-139">MVC service registration</span></span>

<span data-ttu-id="0377b-140">ASP.NET Core 3,0 přidává nové možnosti pro registraci scénářů MVC `Startup.ConfigureServices`v rámci.</span><span class="sxs-lookup"><span data-stu-id="0377b-140">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="0377b-141">K dispozici jsou tři nové metody rozšíření na `IServiceCollection` nejvyšší úrovni související s scénáři MVC.</span><span class="sxs-lookup"><span data-stu-id="0377b-141">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="0377b-142">Šablony používají tyto nové metody místo `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="0377b-142">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="0377b-143">Nicméně se `AddMvc` i nadále chová jako v předchozích verzích.</span><span class="sxs-lookup"><span data-stu-id="0377b-143">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="0377b-144">Následující příklad přidá podporu pro řadiče a funkce související s rozhraním API, ale ne zobrazení nebo stránky.</span><span class="sxs-lookup"><span data-stu-id="0377b-144">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="0377b-145">Šablona rozhraní API používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="0377b-145">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="0377b-146">Následující příklad přidá podporu pro řadiče, funkce související s rozhraním API a zobrazení, ale ne stránky.</span><span class="sxs-lookup"><span data-stu-id="0377b-146">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="0377b-147">Šablona webové aplikace (MVC) používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="0377b-147">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="0377b-148">V následujícím příkladu se přidá podpora pro Razor Pages a minimální Podpora řadičů.</span><span class="sxs-lookup"><span data-stu-id="0377b-148">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="0377b-149">Šablona webové aplikace používá tento kód:</span><span class="sxs-lookup"><span data-stu-id="0377b-149">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="0377b-150">Nové metody lze také kombinovat.</span><span class="sxs-lookup"><span data-stu-id="0377b-150">The new methods can also be combined.</span></span> <span data-ttu-id="0377b-151">Následující příklad je ekvivalentní volání `AddMvc` v ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="0377b-151">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="0377b-152">Aktualizovat spouštěcí kód směrování</span><span class="sxs-lookup"><span data-stu-id="0377b-152">Update routing startup code</span></span>

<span data-ttu-id="0377b-153">Pokud je to aplikace `UseMvc` , `UseSignalR`nebo pokud je to možné, proveďte migraci aplikace do [Směrování koncových bodů](xref:fundamentals/routing) .</span><span class="sxs-lookup"><span data-stu-id="0377b-153">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="0377b-154">Pro zlepšení kompatibility směrování koncových bodů s předchozími verzemi MVC jsme vrátili některé změny v adrese URL představené v ASP.NET Core 2,2.</span><span class="sxs-lookup"><span data-stu-id="0377b-154">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="0377b-155">Pokud jste narazili na problémy s používáním směrování koncových bodů v 2,2, můžete očekávat vylepšení ASP.NET Core 3,0 s následujícími výjimkami:</span><span class="sxs-lookup"><span data-stu-id="0377b-155">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="0377b-156">Pokud aplikace implementuje `IRouter` nebo dědí z `Route`, možná se budete chtít vyhnout migraci v tuto chvíli.</span><span class="sxs-lookup"><span data-stu-id="0377b-156">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="0377b-157">Poskytněte nám svůj názor na [plán pro migraci implementací na základě IRouter do směrování koncových bodů](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="0377b-157">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="0377b-158">Pokud aplikace přímo přistupuje k `RouteData.Routers` ovládacímu prvku MVC, možná se nebudete chtít v tomto okamžiku migrovat.</span><span class="sxs-lookup"><span data-stu-id="0377b-158">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="0377b-159">Poskytněte nám svůj názor na [pokyny k migraci pro použití parametr RouteData. routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="0377b-159">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="0377b-160">Směrování koncového bodu podporuje stejnou syntaxi vzorů směrování a funkce vytváření vzorů směrování jako `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="0377b-160">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="0377b-161">Směrování koncového `IRouteContraint`bodu podporuje.</span><span class="sxs-lookup"><span data-stu-id="0377b-161">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="0377b-162">Směrování koncového `[Route]`bodu `[HttpGet]`podporuje, a další atributy směrování MVC.</span><span class="sxs-lookup"><span data-stu-id="0377b-162">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="0377b-163">Pro většinu aplikací vyžaduje pouze `Startup` změny.</span><span class="sxs-lookup"><span data-stu-id="0377b-163">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="0377b-164">Migrovat Startup. Configure</span><span class="sxs-lookup"><span data-stu-id="0377b-164">Migrate Startup.Configure</span></span>

<span data-ttu-id="0377b-165">Obecné pokyny:</span><span class="sxs-lookup"><span data-stu-id="0377b-165">General advice:</span></span>

* <span data-ttu-id="0377b-166">Přidat `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="0377b-166">Add `UseRouting`.</span></span> 
* <span data-ttu-id="0377b-167">Pokud je aplikace volána `UseStaticFiles`, umístěte `UseStaticFiles` **před** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="0377b-167">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="0377b-168">Pokud aplikace používá funkce `AuthorizePage` ověřování/autorizace `UseAuthentication` , jako je nebo `[Authorize]`, umístěte volání a `UseAuthorization` **After** `UseRouting` (a **po** `UseCors` použití middlewaru CORS).</span><span class="sxs-lookup"><span data-stu-id="0377b-168">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting` (and **after** `UseCors` if CORS Middleware is used).</span></span>
* <span data-ttu-id="0377b-169">`UseMvc` Nahraďte `UseSignalR` nebo s `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="0377b-169">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>
* <span data-ttu-id="0377b-170">Pokud aplikace používá scénáře [CORS](xref:security/cors) , například `[EnableCors]`, umístěte volání do `UseAuthentication` `UseCors` jakýchkoli jiných middlewarů, které používají CORS (například na místo `UseCors` před, `UseAuthorization`a `UseMvc`).</span><span class="sxs-lookup"><span data-stu-id="0377b-170">If the app uses [CORS](xref:security/cors) scenarios, such as `[EnableCors]`, place the call to `UseCors` before any other middlewares that use CORS (for example, place `UseCors` before `UseAuthentication`, `UseAuthorization`, and `UseMvc`).</span></span>
* <span data-ttu-id="0377b-171">Nahraďte `IHostingEnvironment` `using` výrazem a`Microsoft.Extensions.Hosting`přidejte příkaz pro obor názvů. `IWebHostEnvironment`</span><span class="sxs-lookup"><span data-stu-id="0377b-171">Replace `IHostingEnvironment` with `IWebHostEnvironment` and add a `using` statement for the `Microsoft.Extensions.Hosting` namespace.</span></span>

<span data-ttu-id="0377b-172">Následuje příklad `Startup.Configure` typické aplikace ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="0377b-172">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="0377b-173">Po aktualizaci předchozího `Startup.Configure` kódu:</span><span class="sxs-lookup"><span data-stu-id="0377b-173">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="0377b-174">Doprovodné materiály k zabezpečení middlewaru</span><span class="sxs-lookup"><span data-stu-id="0377b-174">Security middleware guidance</span></span>

<span data-ttu-id="0377b-175">Podpora pro autorizaci a CORS je sjednocená v rámci přístupu [middlewaru](xref:fundamentals/middleware/index) .</span><span class="sxs-lookup"><span data-stu-id="0377b-175">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="0377b-176">To umožňuje používat stejný middlewar a funkčnost v těchto scénářích.</span><span class="sxs-lookup"><span data-stu-id="0377b-176">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="0377b-177">V této verzi je k dispozici aktualizovaný middleware autorizace a middleware CORS je vylepšený, aby bylo možné pochopit atributy používané řadiči MVC.</span><span class="sxs-lookup"><span data-stu-id="0377b-177">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="0377b-178">CORS</span><span class="sxs-lookup"><span data-stu-id="0377b-178">CORS</span></span>

<span data-ttu-id="0377b-179">V minulosti mohlo být obtížné nakonfigurovat CORS.</span><span class="sxs-lookup"><span data-stu-id="0377b-179">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="0377b-180">Middleware byl poskytnut pro použití v některých případech použití, ale filtry MVC byly určeny k použití **bez** middlewaru v jiných případech použití.</span><span class="sxs-lookup"><span data-stu-id="0377b-180">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="0377b-181">U ASP.NET Core 3,0 doporučujeme, aby všechny aplikace, které vyžadují CORS, používaly middleware CORS v kombinaci s směrováním koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="0377b-181">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="0377b-182">`UseCors`dá se zadat s výchozí zásadou a `[EnableCors]` `[DisableCors]` pomocí atributů můžete v případě potřeby přepsat výchozí zásady.</span><span class="sxs-lookup"><span data-stu-id="0377b-182">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="0377b-183">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="0377b-183">In the following example:</span></span>

* <span data-ttu-id="0377b-184">U všech koncových bodů s `default` pojmenovanou zásadou je povolená CORS.</span><span class="sxs-lookup"><span data-stu-id="0377b-184">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="0377b-185">Třída zakáže CORS `[DisableCors]` s atributem. `MyController`</span><span class="sxs-lookup"><span data-stu-id="0377b-185">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="0377b-186">Autorizace</span><span class="sxs-lookup"><span data-stu-id="0377b-186">Authorization</span></span>

<span data-ttu-id="0377b-187">V dřívějších verzích ASP.NET Core byla poskytnuta podpora autorizace prostřednictvím `[Authorize]` atributu.</span><span class="sxs-lookup"><span data-stu-id="0377b-187">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="0377b-188">Autorizační middleware není k dispozici.</span><span class="sxs-lookup"><span data-stu-id="0377b-188">Authorization middleware wasn't available.</span></span> <span data-ttu-id="0377b-189">V ASP.NET Core 3,0 se vyžaduje middleware autorizace.</span><span class="sxs-lookup"><span data-stu-id="0377b-189">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="0377b-190">Doporučujeme umístit middleware autorizace ASP.NET Core (`UseAuthorization`) hned po. `UseAuthentication`</span><span class="sxs-lookup"><span data-stu-id="0377b-190">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="0377b-191">Middleware autorizace se dá nakonfigurovat taky s výchozími zásadami, které se dají přepsat.</span><span class="sxs-lookup"><span data-stu-id="0377b-191">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="0377b-192">V ASP.NET Core 3,0 nebo novějším `UseAuthorization` se volá v `Startup.Configure`a následující `HomeController` vyžaduje přihlášeného uživatele:</span><span class="sxs-lookup"><span data-stu-id="0377b-192">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="0377b-193">Pokud aplikace používá `AuthorizeFilter` jako globální filtr v MVC, doporučujeme v `AddAuthorization`volání metody refaktoring kódu, aby poskytovala zásady.</span><span class="sxs-lookup"><span data-stu-id="0377b-193">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy in the call to `AddAuthorization`.</span></span>

<span data-ttu-id="0377b-194">`DefaultPolicy` Je zpočátku nakonfigurovaný tak, aby vyžadoval ověřování, takže se nevyžaduje žádná další konfigurace.</span><span class="sxs-lookup"><span data-stu-id="0377b-194">The `DefaultPolicy` is initially configured to require authentication, so no additional configuration is required.</span></span> <span data-ttu-id="0377b-195">V následujícím příkladu jsou koncové body MVC označeny jako `RequireAuthorization` tak, že všechny požadavky musí být autorizovány na základě. `DefaultPolicy`</span><span class="sxs-lookup"><span data-stu-id="0377b-195">In the following example, MVC endpoints are marked as `RequireAuthorization` so that all requests must be authorized based on the `DefaultPolicy`.</span></span> <span data-ttu-id="0377b-196">Povolit přístup bez přihlášení uživatele do aplikace je však `[AllowAnonymous]`z důvodu: `HomeController`</span><span class="sxs-lookup"><span data-stu-id="0377b-196">However, the `HomeController` allows access without the user signing into the app due to `[AllowAnonymous]`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute().RequireAuthorization();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="0377b-197">Zásady je také možné přizpůsobit.</span><span class="sxs-lookup"><span data-stu-id="0377b-197">Policies can also be customized.</span></span> <span data-ttu-id="0377b-198">Při sestavování v předchozím příkladu `DefaultPolicy` je nakonfigurována tak, aby vyžadovala ověřování a určitý rozsah:</span><span class="sxs-lookup"><span data-stu-id="0377b-198">Building upon the previous example, the `DefaultPolicy` is configured to require authentication and a specific scope:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    ...
    
    services.AddAuthorization(options =>
    {
        options.DefaultPolicy = new AuthorizationPolicyBuilder()
          .RequireAuthenticatedUser()
          .RequireScope("MyScope")
          .Build();
    });
}

public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute().RequireAuthorization();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="0377b-199">Alternativně lze všechny koncové body nakonfigurovat tak, aby vyžadovaly `RequireAuthorization` autorizaci bez `FallbackPolicy` `[Authorize]` nebo konfigurací.</span><span class="sxs-lookup"><span data-stu-id="0377b-199">Alternatively, all endpoints can be configured to require authorization without `[Authorize]` or `RequireAuthorization` by configuring a `FallbackPolicy`.</span></span> <span data-ttu-id="0377b-200">`FallbackPolicy` Se liší `DefaultPolicy`od.</span><span class="sxs-lookup"><span data-stu-id="0377b-200">The `FallbackPolicy` is different from the `DefaultPolicy`.</span></span> <span data-ttu-id="0377b-201">`DefaultPolicy` Spustí`[Authorize]` se nebo ,`RequireAuthorization` Když`FallbackPolicy` se aktivuje, když nejsou nastavené žádné jiné zásady.</span><span class="sxs-lookup"><span data-stu-id="0377b-201">The `DefaultPolicy` is triggered by `[Authorize]` or `RequireAuthorization`, while the `FallbackPolicy` is triggered when no other policy is set.</span></span> <span data-ttu-id="0377b-202">`FallbackPolicy`je zpočátku nakonfigurovaný tak, aby povoloval požadavky bez autorizace.</span><span class="sxs-lookup"><span data-stu-id="0377b-202">`FallbackPolicy` is initially configured to allow requests without authorization.</span></span>

<span data-ttu-id="0377b-203">Následující příklad je stejný jako předchozí `DefaultPolicy` příklad, ale používá, `FallbackPolicy` aby vždy vyžadoval ověřování u všech koncových bodů s výjimkou případů, kdy `[AllowAnonymous]` je zadána:</span><span class="sxs-lookup"><span data-stu-id="0377b-203">The following example is the same as the preceding `DefaultPolicy` example but uses the `FallbackPolicy` to always require authentication on all endpoints except when `[AllowAnonymous]` is specified:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    ...
    services.AddAuthorization(options =>
    {
        options.FallbackPolicy = new AuthorizationPolicyBuilder()
          .RequireAuthenticatedUser()
          .RequireScope("MyScope")
          .Build();
    });
}

public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="0377b-204">Ověřování pomocí middlewaru funguje bez rozhraní, které má konkrétní znalosti o autorizaci.</span><span class="sxs-lookup"><span data-stu-id="0377b-204">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="0377b-205">[Kontroly stavu](xref:host-and-deploy/health-checks) například nemají žádné konkrétní znalosti o autorizaci, ale kontroly stavu můžou mít konfigurovatelné zásady autorizace, které používá middleware.</span><span class="sxs-lookup"><span data-stu-id="0377b-205">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="0377b-206">Každý koncový bod navíc může přizpůsobit své autorizační požadavky.</span><span class="sxs-lookup"><span data-stu-id="0377b-206">Additionally, each endpoint can customize its authorization requirements.</span></span> <span data-ttu-id="0377b-207">V následujícím příkladu `UseAuthorization` zpracovává autorizaci `DefaultPolicy`pomocí nástroje, ale `/healthz` koncový bod kontroly stavu vyžaduje `admin` uživatele:</span><span class="sxs-lookup"><span data-stu-id="0377b-207">In the following example, `UseAuthorization` processes authorization with the `DefaultPolicy`, but the `/healthz` health check endpoint requires an `admin` user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="0377b-208">Ochrana je implementovaná pro některé scénáře.</span><span class="sxs-lookup"><span data-stu-id="0377b-208">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="0377b-209">`UseEndpoint`middleware vyvolá výjimku, pokud je zásada autorizace nebo CORS vynechána z důvodu chybějícího middlewaru.</span><span class="sxs-lookup"><span data-stu-id="0377b-209">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="0377b-210">Podpora analyzátoru, která poskytuje další názory na neplatnou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="0377b-210">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="0377b-211">Migrace signálu</span><span class="sxs-lookup"><span data-stu-id="0377b-211">Migrate SignalR</span></span>

<span data-ttu-id="0377b-212">Mapování rozbočovačů signálu se teď provádí v rámci `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="0377b-212">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="0377b-213">Namapujte každé centrum `MapHub`pomocí.</span><span class="sxs-lookup"><span data-stu-id="0377b-213">Map each hub with `MapHub`.</span></span> <span data-ttu-id="0377b-214">Stejně jako v předchozích verzích je každé centrum výslovně uvedené.</span><span class="sxs-lookup"><span data-stu-id="0377b-214">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="0377b-215">V následujícím příkladu se přidá podpora pro `ChatHub` centrum signalizace:</span><span class="sxs-lookup"><span data-stu-id="0377b-215">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

<span data-ttu-id="0377b-216">Pro řízení omezení velikosti zprávy od klientů je k dispozici nová možnost.</span><span class="sxs-lookup"><span data-stu-id="0377b-216">There is a new option for controlling message size limits from clients.</span></span> <span data-ttu-id="0377b-217">Například v `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="0377b-217">For example, in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddSignalR(hubOptions =>
{
    hubOptions.MaximumReceiveMessageSize = 32768;
});
```

<span data-ttu-id="0377b-218">V ASP.NET Core 2,2 můžete nastavit `TransportMaxBufferSize` a, které by účinně ovládají maximální velikost zprávy.</span><span class="sxs-lookup"><span data-stu-id="0377b-218">In ASP.NET Core 2.2, you could set the `TransportMaxBufferSize` and that would effectively control the maximum message size.</span></span> <span data-ttu-id="0377b-219">V ASP.NET Core 3,0 Tato možnost nyní určuje pouze maximální velikost před nedodržením přítlaku.</span><span class="sxs-lookup"><span data-stu-id="0377b-219">In ASP.NET Core 3.0, that option now only controls the maximum size before backpressure is observed.</span></span>

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="0377b-220">Migrace řadičů MVC</span><span class="sxs-lookup"><span data-stu-id="0377b-220">Migrate MVC controllers</span></span>

<span data-ttu-id="0377b-221">Mapování řadičů se teď provádí v rámci `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="0377b-221">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="0377b-222">Přidejte `MapControllers` , pokud aplikace používá směrování atributů.</span><span class="sxs-lookup"><span data-stu-id="0377b-222">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="0377b-223">Vzhledem k tomu, že směrování zahrnuje podporu pro mnoho platforem v ASP.NET Core 3,0 nebo novějších, je přidání řadičů směrovaných k atributům výslovný souhlas.</span><span class="sxs-lookup"><span data-stu-id="0377b-223">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="0377b-224">Nahraďte následující:</span><span class="sxs-lookup"><span data-stu-id="0377b-224">Replace the following:</span></span>

* <span data-ttu-id="0377b-225">`MapRoute`řetězce`MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="0377b-225">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="0377b-226">`MapAreaRoute`řetězce`MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="0377b-226">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="0377b-227">Vzhledem k tomu, že směrování nyní zahrnuje podporu pro více než jen MVC, se terminologie změnila, aby tyto metody jasně zajistila, co dělají.</span><span class="sxs-lookup"><span data-stu-id="0377b-227">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="0377b-228">Konvenční trasy, například `MapControllerRoute` / `MapAreaControllerRoute` ,jsouaplikoványvpořadí,vjakémbylypřidány.`MapDefaultControllerRoute` /</span><span class="sxs-lookup"><span data-stu-id="0377b-228">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="0377b-229">Nejprve umístěte konkrétnější trasy (například trasy pro oblast).</span><span class="sxs-lookup"><span data-stu-id="0377b-229">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="0377b-230">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="0377b-230">In the following example:</span></span>

* <span data-ttu-id="0377b-231">`MapControllers`Přidá podporu pro řadiče směrované atributy.</span><span class="sxs-lookup"><span data-stu-id="0377b-231">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="0377b-232">`MapAreaControllerRoute`Přidá konvenční trasu pro řadiče v oblasti.</span><span class="sxs-lookup"><span data-stu-id="0377b-232">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="0377b-233">`MapControllerRoute`Přidá konvenční trasu pro řadiče.</span><span class="sxs-lookup"><span data-stu-id="0377b-233">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="0377b-234">Migrace Razor Pages</span><span class="sxs-lookup"><span data-stu-id="0377b-234">Migrate Razor Pages</span></span>

<span data-ttu-id="0377b-235">Mapování Razor Pages nyní probíhá uvnitř `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="0377b-235">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="0377b-236">Přidejte `MapRazorPages` , pokud aplikace používá Razor Pages.</span><span class="sxs-lookup"><span data-stu-id="0377b-236">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="0377b-237">Vzhledem k tomu, že směrování koncových bodů zahrnuje podporu pro spoustu platforem, přidání Razor Pages je nyní výslovným souhlasem.</span><span class="sxs-lookup"><span data-stu-id="0377b-237">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="0377b-238">V následujícím příkladu `MapRazorPages` přidá podporu pro Razor Pages:</span><span class="sxs-lookup"><span data-stu-id="0377b-238">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="0377b-239">Použití MVC bez směrování koncových bodů</span><span class="sxs-lookup"><span data-stu-id="0377b-239">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="0377b-240">Použití MVC prostřednictvím `UseMvc` nebo `UseMvcWithDefaultRoute` v ASP.NET Core 3,0 vyžaduje explicitní výslovný souhlas v rámci `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="0377b-240">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="0377b-241">To je nutné, protože MVC musí zjistit, jestli se může při inicializaci spoléhat na autorizaci a middleware CORS.</span><span class="sxs-lookup"><span data-stu-id="0377b-241">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="0377b-242">K dispozici je analyzátor, který se upozorní, pokud se aplikace pokusí použít nepodporovanou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="0377b-242">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="0377b-243">Pokud aplikace vyžaduje starší `IRouter` verzi podpory, zakažte `EnableEndpointRouting` použití některého z následujících přístupů v `Startup.ConfigureServices`nástroji:</span><span class="sxs-lookup"><span data-stu-id="0377b-243">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="0377b-244">Migrace kontrol stavu</span><span class="sxs-lookup"><span data-stu-id="0377b-244">Migrate health checks</span></span>

<span data-ttu-id="0377b-245">Kontroly stavu lze použít jako *směrovač* s směrováním koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="0377b-245">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="0377b-246">Přidejte `MapHealthChecks` k použití kontrol stavu pomocí směrování koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="0377b-246">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="0377b-247">Metoda přijímá argumenty `UseHealthChecks`podobné. `MapHealthChecks`</span><span class="sxs-lookup"><span data-stu-id="0377b-247">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="0377b-248">Výhodou použití `MapHealthChecks` funkce Over `UseHealthChecks` je možnost použít autorizaci a mít větší jemně odstupňovanou kontrolu nad zásadami pro porovnání.</span><span class="sxs-lookup"><span data-stu-id="0377b-248">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="0377b-249">V následujícím příkladu `MapHealthChecks` je volána pro koncový bod kontroly stavu v `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="0377b-249">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="0377b-250">HostBuilder nahrazuje WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="0377b-250">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="0377b-251">Šablony ASP.NET Core 3,0 používají [obecného hostitele](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="0377b-251">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="0377b-252">Předchozí verze používaly [webového hostitele](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="0377b-252">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="0377b-253">Následující kód ukazuje třídu vygenerovanou `Program` šablonou ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="0377b-253">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="0377b-254">Následující kód ukazuje třídu vygenerovanou `Program` šablonou ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="0377b-254">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="0377b-255"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>zůstává v 3,0 a je typ `webBuilder` zobrazený v předchozí ukázce kódu.</span><span class="sxs-lookup"><span data-stu-id="0377b-255"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="0377b-256"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>bude zastaralá v budoucí verzi a nahrazena nástrojem `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="0377b-256"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="0377b-257">Nejvýznamnější změna z `WebHostBuilder` na `HostBuilder` je v [vkládání závislostí (di)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="0377b-257">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="0377b-258">Při použití `HostBuilder`nástroje lze <xref:Microsoft.Extensions.Configuration.IConfiguration> <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> vložit`Startup`pouze konstruktor.</span><span class="sxs-lookup"><span data-stu-id="0377b-258">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="0377b-259">Omezení `HostBuilder` di:</span><span class="sxs-lookup"><span data-stu-id="0377b-259">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="0377b-260">Povolí sestavení kontejneru DI jenom jednou.</span><span class="sxs-lookup"><span data-stu-id="0377b-260">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="0377b-261">Předejde výsledným problémům životního cyklu objektů, jako je například řešení více instancí singleton.</span><span class="sxs-lookup"><span data-stu-id="0377b-261">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="0377b-262">Aktualizovat kód signálu</span><span class="sxs-lookup"><span data-stu-id="0377b-262">Update SignalR code</span></span>

<span data-ttu-id="0377b-263">`System.Text.Json`je teď výchozím protokolem rozbočovače používaným klientem i serverem.</span><span class="sxs-lookup"><span data-stu-id="0377b-263">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="0377b-264">V `Startup.ConfigureServices`nástroji zavolejte `AddJsonProtocol` na nastavit možnosti serializátoru.</span><span class="sxs-lookup"><span data-stu-id="0377b-264">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="0377b-265">**WebServer**</span><span class="sxs-lookup"><span data-stu-id="0377b-265">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="0377b-266">**Služba**</span><span class="sxs-lookup"><span data-stu-id="0377b-266">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="0377b-267">Přepnout na Newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="0377b-267">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="0377b-268">Pokud používáte funkce `Newtonsoft.Json` nástroje, které nejsou podporované v `System.Text.Json`, můžete přejít zpátky na `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="0377b-268">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="0377b-269">Nainstalujte balíček NuGet [Microsoft. AspNetCore. signaler. Protocols. NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) .</span><span class="sxs-lookup"><span data-stu-id="0377b-269">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="0377b-270">Na straně klienta řetězit `AddNewtonsoftJsonProtocol` volání metody `HubConnectionBuilder` do instance:</span><span class="sxs-lookup"><span data-stu-id="0377b-270">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="0377b-271">Na serveru řetězit `AddNewtonsoftJsonProtocol` volání `AddSignalR` metody volání `Startup.ConfigureServices`metody:</span><span class="sxs-lookup"><span data-stu-id="0377b-271">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="0377b-272">Přihlásit se ke kompilaci za běhu</span><span class="sxs-lookup"><span data-stu-id="0377b-272">Opt in to runtime compilation</span></span>

<span data-ttu-id="0377b-273">V 3,0 je kompilace za běhu v případě výslovného souhlasu.</span><span class="sxs-lookup"><span data-stu-id="0377b-273">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="0377b-274">Chcete-li povolit kompilaci za https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation běhu, přečtěte si téma.</span><span class="sxs-lookup"><span data-stu-id="0377b-274">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
