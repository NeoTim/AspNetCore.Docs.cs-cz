---
title: Migrace z technologie ASP.NET Core 2.2 až 3.0 ve verzi Preview
author: tdykstra
description: Naučte se migrovat projekt ASP.NET Core 2.2 technologie ASP.NET Core 3.0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 1499239ae94919d1b094e6e55970d9920c4da2cd
ms.sourcegitcommit: 8516b586541e6ba402e57228e356639b85dfb2b9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/11/2019
ms.locfileid: "67814871"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="a1a4d-103">Migrace z technologie ASP.NET Core 2.2 až 3.0</span><span class="sxs-lookup"><span data-stu-id="a1a4d-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="a1a4d-104">Podle [Scott Addie](https://github.com/scottaddie) a [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="a1a4d-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="a1a4d-105">Tento článek vysvětluje, jak aktualizovat stávající projekt ASP.NET Core 2.2 technologie ASP.NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a1a4d-106">Požadavky</span><span class="sxs-lookup"><span data-stu-id="a1a4d-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="a1a4d-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="a1a4d-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="a1a4d-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="a1a4d-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="a1a4d-109">Visual Studio pro Mac</span><span class="sxs-lookup"><span data-stu-id="a1a4d-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="a1a4d-110">Aktualizace souboru projektu</span><span class="sxs-lookup"><span data-stu-id="a1a4d-110">Update the project file</span></span>

* <span data-ttu-id="a1a4d-111">Nastavte [cílit na Moniker rozhraní (TFM)](/dotnet/standard/frameworks) k `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="a1a4d-112">Odeberte parametr `<PackageReference>` k [metabalíček](xref:fundamentals/metapackage) nebo [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) Microsoft.aspnetcore.all.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="a1a4d-113">Aktualizace `Version` atribut na zbývající `<PackageReference>` prvky pro `Microsoft.AspNetCore.*` balíčky do aktuální verze preview (například `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="a1a4d-114">Pokud není žádná 3.0 verze balíčku, balíčku může být se již nepoužívají v 3.0.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="a1a4d-115">Mnohé z těchto balíčků jsou součástí `Microsoft.AspNetCore.App` a neměly by být odkazovány jednotlivě.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="a1a4d-116">Předběžný seznam balíčků, které jsou už vytvořené v 3.0 najdete v tématu [zastavit, vytváření balíčků pro sestavení sdílené framework 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="a1a4d-117">*Sdílené architektuře* je sada sestavení ( *.dll* soubory), které jsou na počítači nainstalovaný a odkazuje `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="a1a4d-118">Další informace najdete v tématu [sdílené architektuře](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="a1a4d-119">Sestavení pro několik důležitých komponent, které byly odebrány z `Microsoft.AspNetCore.App` 3.0.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="a1a4d-120">Přidat `<PackageReference>` elementů, pokud používáte rozhraní API z balíčky uvedené v [sestavení odebírán z Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="a1a4d-121">Příklady odebrané komponenty:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="a1a4d-122">Seznam sestavení dodání `Microsoft.AspNetCore.App` nebyl finalizován a změní před 3.0 RTM.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="a1a4d-123">Vezměte v úvahu následující kód:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="a1a4d-124">`ReadAsAsync` Metodu s názvem v předchozím kódu je součástí `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="a1a4d-125">Nainstalujte [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) balíček NuGet pro řešení potíží kompilace v 3.0.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="a1a4d-126">Přidat [Json.NET podporu](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="a1a4d-127">Projekty ve výchozím nastavení [model hostingu v procesu](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) v ASP.NET Core 3.0 nebo novější.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="a1a4d-128">Můžete volitelně odebrat `<AspNetCoreHostingModel>` vlastnost v souboru projektu, pokud je jeho hodnota `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="a1a4d-129">Podpora Json.NET</span><span class="sxs-lookup"><span data-stu-id="a1a4d-129">Json.NET support</span></span>

<span data-ttu-id="a1a4d-130">Jako část práce, kterou [vylepšit rozhraní ASP.NET Core sdílené](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) je odebraná ze sdíleného rozhraní ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="a1a4d-131">Použití Json.NET v projektu aplikace ASP.NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="a1a4d-132">Přidat odkaz na balíček pro [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="a1a4d-133">Aktualizace `Startup.ConfigureServices` volat `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="a1a4d-134">`AddNewtonsoftJson` je kompatibilní s novou metody registrace služby MVC:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="a1a4d-135">Při volání funkce lze nastavit nastavení Json.NET `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="a1a4d-136">Registrace služby MVC</span><span class="sxs-lookup"><span data-stu-id="a1a4d-136">MVC service registration</span></span>

<span data-ttu-id="a1a4d-137">ASP.NET Core 3.0 přidává nové možnosti pro registraci MVC scénáře uvnitř `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="a1a4d-138">Tři nové nejvyšší úrovně rozšiřující metody související s MVC scénáře na `IServiceCollection` jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="a1a4d-139">Šablony používají tyto nové metody `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="a1a4d-140">Ale `AddMvc` i nadále chovat dle má v předchozích verzích.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="a1a4d-141">V následujícím příkladu přidává podporu pro řadiče funkce související se zabezpečením rozhraní API, ale není a zobrazení stránek.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="a1a4d-142">Tento kód používá šablonu rozhraní API:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="a1a4d-143">V následujícím příkladu přidává podporu pro kontrolery, funkce související se zabezpečením rozhraní API a zobrazení, ale nikoli stránky.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="a1a4d-144">Tento kód používá šablony webové aplikace (MVC):</span><span class="sxs-lookup"><span data-stu-id="a1a4d-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="a1a4d-145">Následující příklad přidá podporu pro stránky Razor a minimální kontroleru.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="a1a4d-146">Tento kód používá šablony webové aplikace:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="a1a4d-147">Můžete také kombinovat nové metody.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-147">The new methods can also be combined.</span></span> <span data-ttu-id="a1a4d-148">Následující příklad je ekvivalentní volání `AddMvc` v ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="a1a4d-149">Aktualizace směrování při spuštění kódu</span><span class="sxs-lookup"><span data-stu-id="a1a4d-149">Update routing startup code</span></span>

<span data-ttu-id="a1a4d-150">Pokud aplikace volá `UseMvc` nebo `UseSignalR`, migrovat aplikace do [koncový bod směrování](xref:fundamentals/routing) Pokud je to možné.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="a1a4d-151">Zlepšit směrování koncový bod kompatibilitu s předchozími verzemi MVC, jsme jsme některé změny vrátit zpět v generování adresy URL zavedené v ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="a1a4d-152">Pokud došlo k potížím v 2.2 pomocí směrování koncový bod, očekávejte vylepšení v ASP.NET Core 3.0 s následujícími výjimkami:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="a1a4d-153">Pokud aplikace implementuje `IRouter` nebo dědí z `Route`, můžete chtít vyhnout migrace v tuto chvíli.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a1a4d-154">Poskytnout zpětnou vazbu na [v úmyslu provést migraci implementace IRouter založené na směrování koncový bod](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="a1a4d-155">Pokud má aplikace přístup přímo `RouteData.Routers` uvnitř MVC, můžete chtít vyhnout migrace v tuto chvíli.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="a1a4d-156">Poskytnout zpětnou vazbu na [migrace pokyny k používání RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="a1a4d-157">Koncový bod směrování podporuje stejné syntaxe vzor trasy a vzor cesty pro funkce, jako je vytváření `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="a1a4d-158">Koncový bod podporuje směrování `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="a1a4d-159">Koncový bod podporuje směrování `[Route]`, `[HttpGet]`a ostatní MVC směrovací atributy.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="a1a4d-160">Pro většinu aplikací pouze `Startup` vyžaduje změny.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="a1a4d-161">Migrace Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="a1a4d-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="a1a4d-162">Obecné Rady, jak:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-162">General advice:</span></span>

* <span data-ttu-id="a1a4d-163">Přidat `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="a1a4d-164">Pokud aplikace volá `UseStaticFiles`, umístěte `UseStaticFiles` **před** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="a1a4d-165">Pokud aplikace používá funkce ověřování/autorizace, jako `AuthorizePage` nebo `[Authorize]`, umístěte volání `UseAuthentication` a `UseAuthorization` **po** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="a1a4d-166">Pokud aplikace využívá [CORS](xref:security/cors) funkce, jako `[EnableCors]`, umístěte `UseCors` Další.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="a1a4d-167">Nahraďte `UseMvc` nebo `UseSignalR` s `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="a1a4d-168">Následuje příklad `Startup.Configure` v typické aplikaci ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="a1a4d-169">Po aktualizaci předchozí `Startup.Configure` kódu:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="a1a4d-170">Doprovodné materiály zabezpečení middlewaru</span><span class="sxs-lookup"><span data-stu-id="a1a4d-170">Security middleware guidance</span></span>

<span data-ttu-id="a1a4d-171">Podpora pro autorizaci a CORS je jednotný kolem [middleware](xref:fundamentals/middleware/index) přístup.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="a1a4d-172">To umožňuje používat stejné middleware a funkce v těchto scénářích.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="a1a4d-173">Se aktualizovalo autorizační middlewaru je k dispozici v této verzi a Middlewarem CORS je vylepšená tak, aby mohli srozumitelně atributy řadiče MVC.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="a1a4d-174">CORS</span><span class="sxs-lookup"><span data-stu-id="a1a4d-174">CORS</span></span>

<span data-ttu-id="a1a4d-175">CORS dříve, může být obtížné konfigurace.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="a1a4d-176">Middleware zadaná v slouží v některých případech ale filtry MVC byly určeny k použití **bez** middlewaru v ostatních případech použití.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="a1a4d-177">S ASP.NET Core 3.0 doporučujeme všechny aplikace, které vyžadují CORS používat CORS Middleware v kombinaci s směrování koncový bod.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="a1a4d-178">`UseCors` můžete zadat s výchozími zásadami a `[EnableCors]` a `[DisableCors]` atributy lze použít k přepsání výchozích zásad vyžadováno.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="a1a4d-179">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-179">In the following example:</span></span>

* <span data-ttu-id="a1a4d-180">CORS je povolený pro všechny koncové body s `default` s názvem zásady.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="a1a4d-181">`MyController` Třídy zakáže CORS se `[DisableCors]` atribut.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="a1a4d-182">Autorizace</span><span class="sxs-lookup"><span data-stu-id="a1a4d-182">Authorization</span></span>

<span data-ttu-id="a1a4d-183">V dřívějších verzích sady ASP.NET Core, podporu autorizační poskytla prostřednictvím `[Authorize]` atribut.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="a1a4d-184">Middleware ověřování nebyl k dispozici.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="a1a4d-185">ASP.NET Core 3.0 je vyžadován middleware ověřování.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="a1a4d-186">Doporučujeme umístit Middleware autorizace ASP.NET Core (`UseAuthorization`) okamžitě po `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="a1a4d-187">Middleware ověřování lze také nastavit s výchozími zásadami, které mohou být přepsána nastaveními.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="a1a4d-188">V ASP.NET Core 3.0 nebo novější `UseAuthorization` je volána `Startup.Configure`a následující `HomeController` vyžaduje znaménkem v uživatele:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="a1a4d-189">Pokud aplikace používá `AuthorizeFilter` jako globální filtr v MVC, doporučujeme refaktoring kódu zadejte zásadu, která `UseAuthorization` middlewaru.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="a1a4d-190">V následujícím příkladu, vlastní zásady se použijí na všechny požadavky, když `UseAuthorization` je zavolána a `HomeController` umožňuje přístup uživatele, přihlášení k aplikaci:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="a1a4d-191">Autorizace middlewarem funguje bez rozhraní s jakékoliv zvláštní znalosti autorizace.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="a1a4d-192">Například [kontroly stavu](xref:host-and-deploy/health-checks) nemá žádné zvláštní znalosti autorizace, ale kontroly stavu můžete mít zásadu konfigurovat autorizace použitý middlewarem.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="a1a4d-193">V následujícím příkladu `UseAuthorization` zpracovává autorizace bez výchozí zásady, ale `/healthz` kontroly stavu pomocí koncových bodů vyžaduje, aby uživatel v `admin` role:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="a1a4d-194">Ochrana se implementuje pro několik scénářů.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="a1a4d-195">`UseEndpoint` Middleware vyvolá výjimku, pokud k ověřování nebo zásada CORS se přeskočila z důvodu chybějící middlewaru.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="a1a4d-196">Podpora analyzátoru poskytnout další názor chybné konfigurace právě probíhá.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="a1a4d-197">Migrace SignalR</span><span class="sxs-lookup"><span data-stu-id="a1a4d-197">Migrate SignalR</span></span>

<span data-ttu-id="a1a4d-198">Mapování rozbočovačů SignalR nyní probíhá uvnitř `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a1a4d-199">Mapovat každou centra se `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="a1a4d-200">Stejně jako v předchozích verzích je explicitně uvedené každého rozbočovače.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="a1a4d-201">V následujícím příkladu, podporu `ChatHub` přidá rozbočovače SignalR:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="a1a4d-202">Migrace řadiče MVC</span><span class="sxs-lookup"><span data-stu-id="a1a4d-202">Migrate MVC controllers</span></span>

<span data-ttu-id="a1a4d-203">Mapování řadičů nyní probíhá uvnitř `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="a1a4d-204">Přidat `MapControllers` Pokud aplikace využívá směrováním atributů.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="a1a4d-205">Protože směrování zahrnuje podporu pro mnoho platforem, ASP.NET Core 3.0 nebo novější, přidání atributu směrovat řadičů je vyjádřit výslovný souhlas.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="a1a4d-206">Nahraďte následující:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-206">Replace the following:</span></span>

* <span data-ttu-id="a1a4d-207">`MapRoute` s `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a1a4d-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="a1a4d-208">`MapAreaRoute` s `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="a1a4d-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="a1a4d-209">Protože směrování teď zahrnuje podporu pro víc než jenom MVC, terminologii změnil provádět tyto metody, jasně uvádějí, co dělají.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="a1a4d-210">Konvenční trasy, jako `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` jsou použita popořadě, že jste přidali.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="a1a4d-211">Nejprve umístíte konkrétnější trasy (například trasy pro oblasti).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="a1a4d-212">V následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-212">In the following example:</span></span>

* <span data-ttu-id="a1a4d-213">`MapControllers` Přidá podporu pro atribut směrovat řadiče.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="a1a4d-214">`MapAreaControllerRoute` Přidá konvenční trasu pro kontrolery v oblasti.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="a1a4d-215">`MapControllerRoute` Přidá konvenční trasu pro řadiče.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="a1a4d-216">Migrace pro stránky Razor</span><span class="sxs-lookup"><span data-stu-id="a1a4d-216">Migrate Razor Pages</span></span>

<span data-ttu-id="a1a4d-217">Mapování pro stránky Razor nyní probíhá uvnitř `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="a1a4d-218">Přidat `MapRazorPages` Pokud aplikace používá pro stránky Razor.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="a1a4d-219">Protože směrování koncový bod zahrnuje podporu pro mnoho platforem, přidávání stránek Razor je teď vyjádřit výslovný souhlas.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="a1a4d-220">V následujícím příkladu `MapRazorPages` přidává podporu pro stránky Razor:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="a1a4d-221">Použití MVC bez koncového bodu směrování</span><span class="sxs-lookup"><span data-stu-id="a1a4d-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="a1a4d-222">Pomocí MVC prostřednictvím `UseMvc` nebo `UseMvcWithDefaultRoute` v ASP.NET Core 3.0 vyžaduje explicitní vyjádřit výslovný souhlas uvnitř `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="a1a4d-223">Se totiž MVC musíte vědět, jestli se může spolehnout na autorizaci a Middlewarem CORS během inicializace.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="a1a4d-224">Analyzátor je k dispozici, která vás upozorní, pokud se aplikace pokusí použít má nepodporovanou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="a1a4d-225">Pokud aplikace vyžaduje starší verze `IRouter` podporují, zakažte `EnableEndpointRouting` pomocí kteréhokoli z následujících přístupů v `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="a1a4d-226">Migrace kontroly stavu</span><span class="sxs-lookup"><span data-stu-id="a1a4d-226">Migrate health checks</span></span>

<span data-ttu-id="a1a4d-227">Kontroly stavu může sloužit jako *směrovače hová* se směrováním koncový bod.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="a1a4d-228">Přidat `MapHealthChecks` použití stavu kontroluje se směrováním koncový bod.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="a1a4d-229">`MapHealthChecks` Metoda přijímá argumenty podobný `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="a1a4d-230">Výhodou použití `MapHealthChecks` přes `UseHealthChecks` je schopnost se autorizace a chcete mít větší přesnou kontrolu nad odpovídající zásady.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="a1a4d-231">V následujícím příkladu `MapHealthChecks` se volá pro koncový bod stavu zaškrtnutí na `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="a1a4d-232">HostBuilder replaces WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="a1a4d-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="a1a4d-233">Šablony ASP.NET Core 3.0 používají [obecný hostitele](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="a1a4d-234">Předchozí verze používá [webového hostitele](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="a1a4d-235">Následující kód ukazuje vygenerovanou šablonu ASP.NET Core 3.0 `Program` třídy:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="a1a4d-236">Následující kód ukazuje základní 2.2 technologie ASP.NET, šablona vytvořena `Program` třídy:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="a1a4d-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> zůstane 3.0 a je typu `webBuilder` v předchozím příkladu kódu.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="a1a4d-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> bude v budoucí verzi zastaralá a nahrazuje `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="a1a4d-239">Nejvýznamnější změny z `WebHostBuilder` k `HostBuilder` probíhá [injektáž závislostí (DI)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="a1a4d-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="a1a4d-240">Při použití `HostBuilder`, můžete vložit pouze <xref:Microsoft.Extensions.Configuration.IConfiguration> a <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> do `Startup`v konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="a1a4d-241">`HostBuilder` DI omezení:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="a1a4d-242">Povolte kontejnerů DI být vytvořeny pouze jednou.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="a1a4d-243">Zabraňuje výsledné problémy životnosti objektu jako řešení více instancí jednotlivých prvků.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="a1a4d-244">Aktualizace kódu SignalR</span><span class="sxs-lookup"><span data-stu-id="a1a4d-244">Update SignalR code</span></span>

<span data-ttu-id="a1a4d-245">`System.Text.Json` je teď výchozí centra protokol používaný klientem a serverem.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="a1a4d-246">V `Startup.ConfigureServices`, volání `AddJsonProtocol` nastavení možností serializátor.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="a1a4d-247">**Server:**</span><span class="sxs-lookup"><span data-stu-id="a1a4d-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="a1a4d-248">**Klient:**</span><span class="sxs-lookup"><span data-stu-id="a1a4d-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="a1a4d-249">Přepnout na Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="a1a4d-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="a1a4d-250">Pokud používáte funkce `Newtonsoft.Json` , které nejsou podporovány v `System.Text.Json`, můžete přepnout zpět na `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="a1a4d-251">Nainstalujte [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) balíček NuGet.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="a1a4d-252">Na straně klienta, řetězení `AddNewtonsoftJsonProtocol` volání metod, které `HubConnectionBuilder` instance:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="a1a4d-253">Na serveru pro řetězení `AddNewtonsoftJsonProtocol` volání metod, které `AddSignalR` volání metody `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="a1a4d-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="a1a4d-254">Výslovný souhlas se kompilace modulu runtime</span><span class="sxs-lookup"><span data-stu-id="a1a4d-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="a1a4d-255">Kompilace modulu runtime v 3.0, je scénář opt-in.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="a1a4d-256">Pokud chcete povolit kompilace modulu runtime, naleznete v tématu <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="a1a4d-256">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
