---
title: Přidání ověřování do aplikace ASP.NET Core MVC
author: rick-anderson
description: Postup přidání ověřování do aplikace ASP.NET Core.
ms.author: riande
ms.date: 04/13/2017
uid: tutorials/first-mvc-app/validation
ms.openlocfilehash: 6c59d0188f67872c7dd5599967551d7d390bfdcf
ms.sourcegitcommit: ccbb84ae307a5bc527441d3d509c20b5c1edde05
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/19/2019
ms.locfileid: "65875047"
---
# <a name="add-validation-to-an-aspnet-core-mvc-app"></a><span data-ttu-id="7d0ca-103">Přidání ověřování do aplikace ASP.NET Core MVC</span><span class="sxs-lookup"><span data-stu-id="7d0ca-103">Add validation to an ASP.NET Core MVC app</span></span>

<span data-ttu-id="7d0ca-104">Podle [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="7d0ca-104">By [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="7d0ca-105">V této části:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-105">In this section:</span></span>

* <span data-ttu-id="7d0ca-106">Přidání logiku ověřování k `Movie` modelu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-106">Validation logic is added to the `Movie` model.</span></span>
* <span data-ttu-id="7d0ca-107">Zajistíte tím, že ověřovacích pravidel se vynucují kdykoli uživatel vytvoří nebo upraví videa.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-107">You ensure that the validation rules are enforced any time a user creates or edits a movie.</span></span>

## <a name="keeping-things-dry"></a><span data-ttu-id="7d0ca-108">Udržování věci zkušební</span><span class="sxs-lookup"><span data-stu-id="7d0ca-108">Keeping things DRY</span></span>

<span data-ttu-id="7d0ca-109">Jedním z principů návrhu MVC je [suchého](https://wikipedia.org/wiki/Don%27t_repeat_yourself) ("není opakujte sami").</span><span class="sxs-lookup"><span data-stu-id="7d0ca-109">One of the design tenets of MVC is [DRY](https://wikipedia.org/wiki/Don%27t_repeat_yourself) ("Don't Repeat Yourself").</span></span> <span data-ttu-id="7d0ca-110">ASP.NET Core MVC umožňuje zadat funkci nebo chování pouze jednou a pak jste ji všude, kde se projeví v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-110">ASP.NET Core MVC encourages you to specify functionality or behavior only once, and then have it be reflected everywhere in an app.</span></span> <span data-ttu-id="7d0ca-111">To snižuje množství kódu, které potřebujete k zápisu a provede kód, který napíšete méně chyby náchylné k chybám, usnadňuje testování a jednodušší správu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-111">This reduces the amount of code you need to write and makes the code you do write less error prone, easier to test, and easier to maintain.</span></span>

<span data-ttu-id="7d0ca-112">Podpora ověřování poskytované MVC a Entity Framework Core Code First je dobrým příkladem zásada SUCHÝCH v akci.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-112">The validation support provided by MVC and Entity Framework Core Code First is a good example of the DRY principle in action.</span></span> <span data-ttu-id="7d0ca-113">Ověřovací pravidla můžete zadat pomocí deklarace na jednom místě (ve třídě modelu) a pravidel se vynucují kdekoli v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-113">You can declaratively specify validation rules in one place (in the model class) and the rules are enforced everywhere in the app.</span></span>

[!INCLUDE[](~/includes/RP-MVC/validation.md)]

## <a name="validation-error-ui"></a><span data-ttu-id="7d0ca-114">Chyba ověření uživatelského rozhraní</span><span class="sxs-lookup"><span data-stu-id="7d0ca-114">Validation Error UI</span></span>

<span data-ttu-id="7d0ca-115">Spusťte aplikaci a přejděte na filmy kontroleru.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-115">Run the app and navigate to the Movies controller.</span></span>

<span data-ttu-id="7d0ca-116">Klepněte **vytvořit nový** odkaz na přidání nového videa.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-116">Tap the **Create New** link to add a new movie.</span></span> <span data-ttu-id="7d0ca-117">Vyplňte formulář pomocí některé neplatné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-117">Fill out the form with some invalid values.</span></span> <span data-ttu-id="7d0ca-118">Jakmile jQuery ověřování na straně klienta zjistí chyby, zobrazí chybovou zprávu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-118">As soon as jQuery client side validation detects the error, it displays an error message.</span></span>

![Video zobrazit formulář s více chyby ověření na straně klienta jQuery](~/tutorials/first-mvc-app/validation/_static/val.png)

[!INCLUDE[](~/includes/currency.md)]

<span data-ttu-id="7d0ca-120">Všimněte si, jak formulář automaticky vykreslí příslušné ověřovací chybovou zprávu do každého pole, který obsahuje neplatnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-120">Notice how the form has automatically rendered an appropriate validation error message in each field containing an invalid value.</span></span> <span data-ttu-id="7d0ca-121">Chyby se vynucují straně klienta (pomocí jazyků JavaScript a jQuery) a na straně serveru (v případě má zakázaný JavaScript).</span><span class="sxs-lookup"><span data-stu-id="7d0ca-121">The errors are enforced both client-side (using JavaScript and jQuery) and server-side (in case a user has JavaScript disabled).</span></span>

<span data-ttu-id="7d0ca-122">Významné výhodou je, že nemusíte změnit jediný řádek kódu v `MoviesController` třídy nebo *Create.cshtml* zobrazení, chcete-li povolit toto ověření uživatelského rozhraní.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-122">A significant benefit is that you didn't need to change a single line of code in the `MoviesController` class or in the *Create.cshtml* view in order to enable this validation UI.</span></span> <span data-ttu-id="7d0ca-123">Kontroler a zobrazení, které jste vytvořili dříve v tomto kurzu automaticky vybere nahoru ověřovací pravidla, které jste zadali pomocí atributů ověření na vlastnosti `Movie` třída modelu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-123">The controller and views you created earlier in this tutorial automatically picked up the validation rules that you specified by using validation attributes on the properties of the `Movie` model class.</span></span> <span data-ttu-id="7d0ca-124">Test ověření pomocí `Edit` je použít metody akce a stejné ověřování.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-124">Test validation using the `Edit` action method, and the same validation is applied.</span></span>

<span data-ttu-id="7d0ca-125">Data formuláře neodešle na server, dokud nedojde k žádným chybám ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-125">The form data isn't sent to the server until there are no client side validation errors.</span></span> <span data-ttu-id="7d0ca-126">Můžete to ověřit tak, že vložíte přerušení `HTTP Post` metodu, pomocí [nástroj Fiddler](http://www.telerik.com/fiddler) , nebo [nástroje pro vývojáře F12](/microsoft-edge/devtools-guide).</span><span class="sxs-lookup"><span data-stu-id="7d0ca-126">You can verify this by putting a break point in the `HTTP Post` method, by using the [Fiddler tool](http://www.telerik.com/fiddler) , or the [F12 Developer tools](/microsoft-edge/devtools-guide).</span></span>

## <a name="how-validation-works"></a><span data-ttu-id="7d0ca-127">Jak funguje ověřování</span><span class="sxs-lookup"><span data-stu-id="7d0ca-127">How validation works</span></span>

<span data-ttu-id="7d0ca-128">Může vás zajímat, jak byl vygenerován ověření uživatelského rozhraní bez jakýchkoli aktualizací kódu v kontroleru nebo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-128">You might wonder how the validation UI was generated without any updates to the code in the controller or views.</span></span> <span data-ttu-id="7d0ca-129">Následující kód ukazuje dva `Create` metody.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-129">The following code shows the two `Create` methods.</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc//sample/MvcMovie/Controllers/MoviesController.cs?name=snippetCreate)]

<span data-ttu-id="7d0ca-130">První (HTTP GET) `Create` zobrazí úvodní formulář vytvořit metody akce.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-130">The first (HTTP GET) `Create` action method displays the initial Create form.</span></span> <span data-ttu-id="7d0ca-131">Druhý (`[HttpPost]`) verze zpracovává odeslaného formuláře.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-131">The second (`[HttpPost]`) version handles the form post.</span></span> <span data-ttu-id="7d0ca-132">Druhá `Create` – metoda ( `[HttpPost]` verze) volání `ModelState.IsValid` ke kontrole, jestli má tento film všechny chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-132">The second `Create` method (The `[HttpPost]` version) calls `ModelState.IsValid` to check whether the movie has any validation errors.</span></span> <span data-ttu-id="7d0ca-133">Voláním této metody vyhodnotí všechny atributy ověření, které se použily k objektu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-133">Calling this method evaluates any validation attributes that have been applied to the object.</span></span> <span data-ttu-id="7d0ca-134">Pokud objekt má chyby ověření, `Create` metoda znovu zobrazí formulář.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-134">If the object has validation errors, the `Create` method re-displays the form.</span></span> <span data-ttu-id="7d0ca-135">Pokud nejsou žádné chyby, metoda tento nový film uloží v databázi.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-135">If there are no errors, the method saves the new movie in the database.</span></span> <span data-ttu-id="7d0ca-136">V našem příkladu film formuláři není odeslána na server po ověření chyby zjištěné na straně klienta; Druhá `Create` metoda se nikdy nevolá, když jsou chyby ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-136">In our movie example, the form isn't posted to the server when there are validation errors detected on the client side; the second `Create` method is never called when there are client side validation errors.</span></span> <span data-ttu-id="7d0ca-137">Pokud zakážete jazyka JavaScript v prohlížeči, je zakázáno ověřování na straně klienta a budete moci otestovat HTTP POST `Create` metoda `ModelState.IsValid` detekce všech chyb ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-137">If you disable JavaScript in your browser, client validation is disabled and you can test the HTTP POST `Create` method `ModelState.IsValid` detecting any validation errors.</span></span>

<span data-ttu-id="7d0ca-138">Lze nastavit bod přerušení v `[HttpPost] Create` metoda a ověřit se nikdy nevolá metodu, ověřování na straně klienta nebude odeslat data formuláře, když se zjistí chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-138">You can set a break point in the `[HttpPost] Create` method and verify the method is never called, client side validation won't submit the form data when validation errors are detected.</span></span> <span data-ttu-id="7d0ca-139">Pokud zakážete jazyka JavaScript v prohlížeči a pak odesláním formuláře s chybami, se setkají bodu přerušení.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-139">If you disable JavaScript in your browser, then submit the form with errors, the break point will be hit.</span></span> <span data-ttu-id="7d0ca-140">Vy i přesto úplného ověření bez jazyka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-140">You still get full validation without JavaScript.</span></span> 

<span data-ttu-id="7d0ca-141">Následující obrázek ukazuje, jak zakázat jazyka JavaScript v prohlížeči FireFox.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-141">The following image shows how to disable JavaScript in the FireFox browser.</span></span>

![Firefox: Na kartě obsah z možností zrušte zaškrtnutí políčka Povolit Javascript.](~/tutorials/first-mvc-app/validation/_static/ff.png)

<span data-ttu-id="7d0ca-143">Následující obrázek ukazuje, jak zakázat jazyka JavaScript v prohlížeči Chrome.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-143">The following image shows how to disable JavaScript in the Chrome browser.</span></span>

![Google Chrome: V části Nastavení obsahu Javascript neumožňují vybrat libovolnou lokalitu spouštět JavaScript.](~/tutorials/first-mvc-app/validation/_static/chrome.png)

<span data-ttu-id="7d0ca-145">Po zakázání JavaScript odeslat neplatná data a projít ladicí program.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-145">After you disable JavaScript, post invalid data and step through the debugger.</span></span>

![Při ladění na příspěvek neplatných dat, technologie Intellisense v ModelState.IsValid ukazuje, že je hodnota false.](~/tutorials/first-mvc-app/validation/_static/ms.png)

<span data-ttu-id="7d0ca-147">Část *Create.cshtml* zobrazení šablony se zobrazí v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-147">The portion of the *Create.cshtml* view template is shown in the following markup:</span></span>

[!code-HTML[](~/tutorials/first-mvc-app/start-mvc/sample/MvcMovie22/Views/Movies/CreateRatingBrevity.html)]

<span data-ttu-id="7d0ca-148">Předchozí kód používá metody akce k počáteční formulář pro zobrazení a znovu ji v případě chyby.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-148">The preceding markup is used by the action methods to display the initial form and to redisplay it in the event of an error.</span></span>

<span data-ttu-id="7d0ca-149">[Pomocné rutiny značky vstup](xref:mvc/views/working-with-forms) používá [DataAnnotations](/aspnet/mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-6) atributy a vytváří atributy HTML, které jsou potřeba pro architekturu jQuery ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-149">The [Input Tag Helper](xref:mvc/views/working-with-forms) uses the [DataAnnotations](/aspnet/mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-6) attributes and produces HTML attributes needed for jQuery Validation on the client side.</span></span> <span data-ttu-id="7d0ca-150">[Pomocné rutiny značky ověření](xref:mvc/views/working-with-forms#the-validation-tag-helpers) zobrazí chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-150">The [Validation Tag Helper](xref:mvc/views/working-with-forms#the-validation-tag-helpers) displays validation errors.</span></span> <span data-ttu-id="7d0ca-151">Zobrazit [ověření](xref:mvc/models/validation) Další informace.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-151">See [Validation](xref:mvc/models/validation) for more information.</span></span>

<span data-ttu-id="7d0ca-152">Co je hodně příjemné o tento přístup je, že žádná z kontroleru ani `Create` zobrazit šablonu nic ví o skutečné ověřovacích pravidel se vynucují nebo specifické chybové zprávy zobrazeny.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-152">What's really nice about this approach is that neither the controller nor the `Create` view template knows anything about the actual validation rules being enforced or about the specific error messages displayed.</span></span> <span data-ttu-id="7d0ca-153">Ověřovací pravidla a chybové řetězce se zadávají pouze v `Movie` třídy.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-153">The validation rules and the error strings are specified only in the `Movie` class.</span></span> <span data-ttu-id="7d0ca-154">Tyto stejné ověřovací pravidla se automaticky využije na `Edit` zobrazení a jakékoli další zobrazení šablony můžete vytvořit, upravit model.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-154">These same validation rules are automatically applied to the `Edit` view and any other views templates you might create that edit your model.</span></span>

<span data-ttu-id="7d0ca-155">Pokud potřebujete změnit logiku ověřování, lze provést přesně pohromadě přidáním atributů ověření do modelu (v tomto příkladu `Movie` třídy).</span><span class="sxs-lookup"><span data-stu-id="7d0ca-155">When you need to change validation logic, you can do so in exactly one place by adding validation attributes to the model (in this example, the `Movie` class).</span></span> <span data-ttu-id="7d0ca-156">Není nutné se starat o různé části aplikace, s jakým způsobem se vynucují pravidla nekonzistentní – veškerou logiku ověřování definované na jednom místě, který se použít kdekoli.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-156">You won't have to worry about different parts of the application being inconsistent with how the rules are enforced — all validation logic will be defined in one place and used everywhere.</span></span> <span data-ttu-id="7d0ca-157">To zajišťuje velmi čistým kódem a umožňuje snadno vyvíjet a udržovat.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-157">This keeps the code very clean, and makes it easy to maintain and evolve.</span></span> <span data-ttu-id="7d0ca-158">A to znamená, že je budete mít plně dodržením zásada SUCHÝCH.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-158">And it means that you'll be fully honoring the DRY principle.</span></span>

## <a name="using-datatype-attributes"></a><span data-ttu-id="7d0ca-159">Pomocí atributů datový typ</span><span class="sxs-lookup"><span data-stu-id="7d0ca-159">Using DataType Attributes</span></span>

<span data-ttu-id="7d0ca-160">Otevřít *Movie.cs* souboru a zkoumat `Movie` třídy.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-160">Open the *Movie.cs* file and examine the `Movie` class.</span></span> <span data-ttu-id="7d0ca-161">`System.ComponentModel.DataAnnotations` Obor názvů obsahuje atributy formátování kromě integrovaná sada atributů ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-161">The `System.ComponentModel.DataAnnotations` namespace provides formatting attributes in addition to the built-in set of validation attributes.</span></span> <span data-ttu-id="7d0ca-162">Už jsme použili `DataType` hodnota výčtu datum vydání a na pole price.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-162">We've already applied a `DataType` enumeration value to the release date and to the price fields.</span></span> <span data-ttu-id="7d0ca-163">Následující kód ukazuje `ReleaseDate` a `Price` vlastnosti příslušnou `DataType` atribut.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-163">The following code shows the `ReleaseDate` and `Price` properties with the appropriate `DataType` attribute.</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc//sample/MvcMovie/Models/MovieDateRatingDA.cs?highlight=2,6&name=snippet2)]

<span data-ttu-id="7d0ca-164">`DataType` Atributy pouze poskytnout nápovědu pro modul zobrazení pro zobrazení dat (a poskytuje elementy nebo atributy, jako `<a>` pro adresy URL a `<a href="mailto:EmailAddress.com">` e-mailu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-164">The `DataType` attributes only provide hints for the view engine to format the data (and supplies elements/attributes such as `<a>` for URL's and `<a href="mailto:EmailAddress.com">` for email.</span></span> <span data-ttu-id="7d0ca-165">Můžete použít `RegularExpression` atribut pro ověření formátu data.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-165">You can use the `RegularExpression` attribute to validate the format of the data.</span></span> <span data-ttu-id="7d0ca-166">`DataType` Atribut se používá k určení datový typ, který je specifičtější než vnitřní typ databáze, nejsou atributů ověření.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-166">The `DataType` attribute is used to specify a data type that's more specific than the database intrinsic type, they're not validation attributes.</span></span> <span data-ttu-id="7d0ca-167">V tomto případě chceme jenom udržovat přehled o datum bez času.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-167">In this case we only want to keep track of the date, not the time.</span></span> <span data-ttu-id="7d0ca-168">`DataType` Výčet poskytuje pro mnoho typů dat, jako je například datum, čas, telefonní číslo, Měna, EmailAddress a další.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-168">The `DataType` Enumeration provides for many data types, such as Date, Time, PhoneNumber, Currency, EmailAddress and more.</span></span> <span data-ttu-id="7d0ca-169">`DataType` Atribut můžete také povolit aplikace a zajistit tak automaticky specifické pro typ funkce.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-169">The `DataType` attribute can also enable the application to automatically provide type-specific features.</span></span> <span data-ttu-id="7d0ca-170">Například `mailto:` propojení lze vytvořit pro `DataType.EmailAddress`, a selektor date lze zadat pro `DataType.Date` v prohlížečích podporujících HTML5.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-170">For example, a `mailto:` link can be created for `DataType.EmailAddress`, and a date selector can be provided for `DataType.Date` in browsers that support HTML5.</span></span> <span data-ttu-id="7d0ca-171">`DataType` Generování atributů HTML 5 `data-` (čteno data dash) atributy, které umožní pochopit prohlížeče HTML 5.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-171">The `DataType` attributes emit HTML 5 `data-` (pronounced data dash) attributes that HTML 5 browsers can understand.</span></span> <span data-ttu-id="7d0ca-172">`DataType` Proveďte atributy **není** žádné ověřování.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-172">The `DataType` attributes do **not** provide any validation.</span></span>

<span data-ttu-id="7d0ca-173">`DataType.Date` neurčuje formátu, který se zobrazí datum.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-173">`DataType.Date` doesn't specify the format of the date that's displayed.</span></span> <span data-ttu-id="7d0ca-174">Ve výchozím nastavení, zobrazí se pole data podle výchozí formát založený na serveru `CultureInfo`.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-174">By default, the data field is displayed according to the default formats based on the server's `CultureInfo`.</span></span>

<span data-ttu-id="7d0ca-175">`DisplayFormat` Atribut se používá s ohledem na formát data:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-175">The `DisplayFormat` attribute is used to explicitly specify the date format:</span></span>

```csharp
[DisplayFormat(DataFormatString = "{0:yyyy-MM-dd}", ApplyFormatInEditMode = true)]
public DateTime ReleaseDate { get; set; }
```

<span data-ttu-id="7d0ca-176">`ApplyFormatInEditMode` Nastavení určuje, že formátování také bude použito při hodnota se zobrazí v textovém poli pro úpravu.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-176">The `ApplyFormatInEditMode` setting specifies that the formatting should also be applied when the value is displayed in a text box for editing.</span></span> <span data-ttu-id="7d0ca-177">(Pokud nechcete pro některá pole – například pro hodnoty měny, pravděpodobně nechcete symbol měny v textovém poli pro úpravu.)</span><span class="sxs-lookup"><span data-stu-id="7d0ca-177">(You might not want that for some fields — for example, for currency values, you probably don't want the currency symbol in the text box for editing.)</span></span>

<span data-ttu-id="7d0ca-178">Můžete použít `DisplayFormat` atribut samotný, ale to je obecně vhodné použít `DataType` atribut.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-178">You can use the `DisplayFormat` attribute by itself, but it's generally a good idea to use the `DataType` attribute.</span></span> <span data-ttu-id="7d0ca-179">`DataType` Atribut přenáší sémantiku dat na rozdíl od vykreslování na obrazovce a nabízí následující výhody, které vám s DisplayFormat:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-179">The `DataType` attribute conveys the semantics of the data as opposed to how to render it on a screen, and provides the following benefits that you don't get with DisplayFormat:</span></span>

* <span data-ttu-id="7d0ca-180">Prohlížeči můžete povolit funkce HTML5 (pro příklad, který znázorňuje ovládacího prvku kalendář, symbol měny odpovídající národní prostředí, odkazy na e-mailu, atd.)</span><span class="sxs-lookup"><span data-stu-id="7d0ca-180">The browser can enable HTML5 features (for example to show a calendar control, the locale-appropriate currency symbol, email links, etc.)</span></span>

* <span data-ttu-id="7d0ca-181">Ve výchozím prohlížeči bude vykreslovat data ve správném formátu podle vašeho národního prostředí.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-181">By default, the browser will render data using the correct format based on your locale.</span></span>

* <span data-ttu-id="7d0ca-182">`DataType` Atribut můžete povolit MVC zvolit šablonu pravé pole k poskytnutí dat ( `DisplayFormat` pokud používá sama používá šablony řetězce).</span><span class="sxs-lookup"><span data-stu-id="7d0ca-182">The `DataType` attribute can enable MVC to choose the right field template to render the data (the `DisplayFormat` if used by itself uses the string template).</span></span>

> [!NOTE]
> <span data-ttu-id="7d0ca-183">k ověřování jQuery nefunguje při využití `Range` atribut a `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-183">jQuery validation doesn't work with the `Range` attribute and `DateTime`.</span></span> <span data-ttu-id="7d0ca-184">Například následující kód se vždy zobrazí chyba ověření na straně klienta, i v případě, že datum je v zadaném rozsahu:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-184">For example, the following code will always display a client side validation error, even when the date is in the specified range:</span></span>
>
> `[Range(typeof(DateTime), "1/1/1966", "1/1/2020")]`

<span data-ttu-id="7d0ca-185">Je potřeba zakázat ověřování jQuery data použít `Range` atributem `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-185">You will need to disable jQuery date validation to use the `Range` attribute with `DateTime`.</span></span> <span data-ttu-id="7d0ca-186">Není obvykle vhodné pro kompilaci pevného data ve vašich modelů použít `Range` atribut a `DateTime` se nedoporučuje.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-186">It's generally not a good practice to compile hard dates in your models, so using the `Range` attribute and `DateTime` is discouraged.</span></span>

<span data-ttu-id="7d0ca-187">Následující kód ukazuje kombinace atributů na jednom řádku:</span><span class="sxs-lookup"><span data-stu-id="7d0ca-187">The following code shows combining attributes on one line:</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc/sample/MvcMovie22/Models/MovieDateRatingDAmult.cs?name=snippet1)]

<span data-ttu-id="7d0ca-188">V další části této série jsme zkontrolujte aplikaci a proveďte několik vylepšení pro automaticky generované `Details` a `Delete` metody.</span><span class="sxs-lookup"><span data-stu-id="7d0ca-188">In the next part of the series, we review the app and make some improvements to the automatically generated `Details` and `Delete` methods.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="7d0ca-189">Další zdroje</span><span class="sxs-lookup"><span data-stu-id="7d0ca-189">Additional resources</span></span>

* [<span data-ttu-id="7d0ca-190">Práce s formuláři</span><span class="sxs-lookup"><span data-stu-id="7d0ca-190">Working with Forms</span></span>](xref:mvc/views/working-with-forms)
* [<span data-ttu-id="7d0ca-191">Globalizace a lokalizace</span><span class="sxs-lookup"><span data-stu-id="7d0ca-191">Globalization and localization</span></span>](xref:fundamentals/localization)
* [<span data-ttu-id="7d0ca-192">Úvod do pomocné rutiny značek</span><span class="sxs-lookup"><span data-stu-id="7d0ca-192">Introduction to Tag Helpers</span></span>](xref:mvc/views/tag-helpers/intro)
* [<span data-ttu-id="7d0ca-193">Autor pomocných rutin značek</span><span class="sxs-lookup"><span data-stu-id="7d0ca-193">Author Tag Helpers</span></span>](xref:mvc/views/tag-helpers/authoring)

> [!div class="step-by-step"]
> <span data-ttu-id="7d0ca-194">[Předchozí](new-field.md)
> [další](details.md)</span><span class="sxs-lookup"><span data-stu-id="7d0ca-194">[Previous](new-field.md)
[Next](details.md)</span></span>  
