---
title: Přidání ověření do aplikace core mvc ASP.NET
author: rick-anderson
description: Jak přidat ověření do aplikace ASP.NET Core.
ms.author: riande
ms.date: 04/13/2017
uid: tutorials/first-mvc-app/validation
ms.openlocfilehash: ecf3d011b38347eb32020df00e44d93ca789443a
ms.sourcegitcommit: f7886fd2e219db9d7ce27b16c0dc5901e658d64e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/06/2020
ms.locfileid: "80242533"
---
# <a name="add-validation-to-an-aspnet-core-mvc-app"></a><span data-ttu-id="d9ac5-103">Přidání ověření do aplikace core mvc ASP.NET</span><span class="sxs-lookup"><span data-stu-id="d9ac5-103">Add validation to an ASP.NET Core MVC app</span></span>

<span data-ttu-id="d9ac5-104">Autor: [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="d9ac5-104">By [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="d9ac5-105">V této části:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-105">In this section:</span></span>

* <span data-ttu-id="d9ac5-106">Logika ověření je `Movie` přidána do modelu.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-106">Validation logic is added to the `Movie` model.</span></span>
* <span data-ttu-id="d9ac5-107">Zajistíte, aby ověřovací pravidla byla vynucena při každém vytvoření nebo úpravě filmu uživatelem.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-107">You ensure that the validation rules are enforced any time a user creates or edits a movie.</span></span>

## <a name="keeping-things-dry"></a><span data-ttu-id="d9ac5-108">Udržování věcí v suchu</span><span class="sxs-lookup"><span data-stu-id="d9ac5-108">Keeping things DRY</span></span>

<span data-ttu-id="d9ac5-109">Jedním z konstrukčních principů MVC je [DRY](https://wikipedia.org/wiki/Don%27t_repeat_yourself) ("Neopakujte se").</span><span class="sxs-lookup"><span data-stu-id="d9ac5-109">One of the design tenets of MVC is [DRY](https://wikipedia.org/wiki/Don%27t_repeat_yourself) ("Don't Repeat Yourself").</span></span> <span data-ttu-id="d9ac5-110">ASP.NET Core MVC doporučuje zadat funkce nebo chování pouze jednou a pak se projeví všude v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-110">ASP.NET Core MVC encourages you to specify functionality or behavior only once, and then have it be reflected everywhere in an app.</span></span> <span data-ttu-id="d9ac5-111">To snižuje množství kódu, který potřebujete k zápisu a umožňuje kód, který píšete méně náchylné k chybám, snadněji testovat a snadněji udržovat.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-111">This reduces the amount of code you need to write and makes the code you do write less error prone, easier to test, and easier to maintain.</span></span>

<span data-ttu-id="d9ac5-112">Podpora validace poskytovaná společností MVC a core code frameworkem First je dobrým příkladem principu DRY v akci.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-112">The validation support provided by MVC and Entity Framework Core Code First is a good example of the DRY principle in action.</span></span> <span data-ttu-id="d9ac5-113">Můžete deklarativně zadat ověřovací pravidla na jednom místě (ve třídě modelu) a pravidla jsou vynucena všude v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-113">You can declaratively specify validation rules in one place (in the model class) and the rules are enforced everywhere in the app.</span></span>

[!INCLUDE[](~/includes/RP-MVC/validation.md)]

## <a name="validation-error-ui"></a><span data-ttu-id="d9ac5-114">Urozhraní s chybou ověření</span><span class="sxs-lookup"><span data-stu-id="d9ac5-114">Validation Error UI</span></span>

<span data-ttu-id="d9ac5-115">Spusťte aplikaci a přejděte na ovladač Filmy.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-115">Run the app and navigate to the Movies controller.</span></span>

<span data-ttu-id="d9ac5-116">Klepnutím na odkaz **Vytvořit nový** přidáte nový film.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-116">Tap the **Create New** link to add a new movie.</span></span> <span data-ttu-id="d9ac5-117">Vyplňte formulář s některými neplatnými hodnotami.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-117">Fill out the form with some invalid values.</span></span> <span data-ttu-id="d9ac5-118">Jakmile ověření na straně klienta jQuery zjistí chybu, zobrazí se chybová zpráva.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-118">As soon as jQuery client side validation detects the error, it displays an error message.</span></span>

![Formulář zobrazení filmu s více chybami ověření na straně klienta jQuery](~/tutorials/first-mvc-app/validation/_static/val.png)

[!INCLUDE[](~/includes/localization/currency.md)]

<span data-ttu-id="d9ac5-120">Všimněte si, jak formulář automaticky vykreslil příslušnou chybovou zprávu ověření v každém poli obsahující neplatnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-120">Notice how the form has automatically rendered an appropriate validation error message in each field containing an invalid value.</span></span> <span data-ttu-id="d9ac5-121">Chyby jsou vynuceny jak na straně klienta (pomocí JavaScriptu a jQuery), tak na straně serveru (v případě, že má uživatel JavaScript zakázán).</span><span class="sxs-lookup"><span data-stu-id="d9ac5-121">The errors are enforced both client-side (using JavaScript and jQuery) and server-side (in case a user has JavaScript disabled).</span></span>

<span data-ttu-id="d9ac5-122">Významnou výhodou je, že jste nemuseli měnit jeden `MoviesController` řádek kódu ve třídě nebo v zobrazení *Create.cshtml,* abyste mohli povolit toto ověřovací ui.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-122">A significant benefit is that you didn't need to change a single line of code in the `MoviesController` class or in the *Create.cshtml* view in order to enable this validation UI.</span></span> <span data-ttu-id="d9ac5-123">Řadič a zobrazení, které jste vytvořili dříve v tomto kurzu automaticky zvedl ověřovací pravidla, která jste zadali pomocí atributů ověření na vlastnosti třídy `Movie` modelu.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-123">The controller and views you created earlier in this tutorial automatically picked up the validation rules that you specified by using validation attributes on the properties of the `Movie` model class.</span></span> <span data-ttu-id="d9ac5-124">Test ověření `Edit` pomocí metody akce a stejné ověření se použije.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-124">Test validation using the `Edit` action method, and the same validation is applied.</span></span>

<span data-ttu-id="d9ac5-125">Data formuláře nejsou odeslána na server, dokud neexistují žádné chyby ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-125">The form data isn't sent to the server until there are no client side validation errors.</span></span> <span data-ttu-id="d9ac5-126">Můžete to ověřit umístěním bodu `HTTP Post` přerušení metody pomocí [nástroje Fiddler](https://www.telerik.com/fiddler) nebo [nástrojů pro vývojáře F12](/microsoft-edge/devtools-guide).</span><span class="sxs-lookup"><span data-stu-id="d9ac5-126">You can verify this by putting a break point in the `HTTP Post` method, by using the [Fiddler tool](https://www.telerik.com/fiddler) , or the [F12 Developer tools](/microsoft-edge/devtools-guide).</span></span>

## <a name="how-validation-works"></a><span data-ttu-id="d9ac5-127">Jak ověření funguje</span><span class="sxs-lookup"><span data-stu-id="d9ac5-127">How validation works</span></span>

<span data-ttu-id="d9ac5-128">Možná se divíte, jak bylo generováno ověřovací umělá vláda bez jakýchkoli aktualizací kódu v kontroleru nebo zobrazeních.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-128">You might wonder how the validation UI was generated without any updates to the code in the controller or views.</span></span> <span data-ttu-id="d9ac5-129">Následující kód ukazuje `Create` dvě metody.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-129">The following code shows the two `Create` methods.</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc//sample/MvcMovie/Controllers/MoviesController.cs?name=snippetCreate)]

<span data-ttu-id="d9ac5-130">První metoda akce `Create` (HTTP GET) zobrazí počáteční formulář Create.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-130">The first (HTTP GET) `Create` action method displays the initial Create form.</span></span> <span data-ttu-id="d9ac5-131">Druhá verze`[HttpPost]`( ) zpracovává formulářový příspěvek.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-131">The second (`[HttpPost]`) version handles the form post.</span></span> <span data-ttu-id="d9ac5-132">Druhá `Create` metoda (Verze) `[HttpPost]` `ModelState.IsValid` volá ke kontrole, zda film má nějaké chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-132">The second `Create` method (The `[HttpPost]` version) calls `ModelState.IsValid` to check whether the movie has any validation errors.</span></span> <span data-ttu-id="d9ac5-133">Volání této metody vyhodnotí všechny ověřovací atributy, které byly použity na objekt.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-133">Calling this method evaluates any validation attributes that have been applied to the object.</span></span> <span data-ttu-id="d9ac5-134">Pokud má objekt chyby `Create` ověření, metoda znovu zobrazí formulář.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-134">If the object has validation errors, the `Create` method re-displays the form.</span></span> <span data-ttu-id="d9ac5-135">Pokud nejsou žádné chyby, metoda uloží nový film v databázi.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-135">If there are no errors, the method saves the new movie in the database.</span></span> <span data-ttu-id="d9ac5-136">V našem příkladu filmu není formulář zaúčtován na server, pokud jsou na straně klienta zjištěny chyby ověření; druhá `Create` metoda je nikdy volána, pokud existují chyby ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-136">In our movie example, the form isn't posted to the server when there are validation errors detected on the client side; the second `Create` method is never called when there are client side validation errors.</span></span> <span data-ttu-id="d9ac5-137">Pokud v prohlížeči zakážete JavaScript, ověření klienta `Create` `ModelState.IsValid` je zakázáno a můžete otestovat metodu HTTP POST, která zjistí chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-137">If you disable JavaScript in your browser, client validation is disabled and you can test the HTTP POST `Create` method `ModelState.IsValid` detecting any validation errors.</span></span>

<span data-ttu-id="d9ac5-138">Můžete nastavit bod přerušení `[HttpPost] Create` v metodě a ověřit, že metoda není nikdy volána, ověření na straně klienta nebude odesílat data formuláře při zjištění chyb ověření.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-138">You can set a break point in the `[HttpPost] Create` method and verify the method is never called, client side validation won't submit the form data when validation errors are detected.</span></span> <span data-ttu-id="d9ac5-139">Pokud ve svém prohlížeči zakážete JavaScript a poté odešlete formulář s chybami, bude bod přerušení zasažen.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-139">If you disable JavaScript in your browser, then submit the form with errors, the break point will be hit.</span></span> <span data-ttu-id="d9ac5-140">Stále získáte úplné ověření bez JavaScriptu.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-140">You still get full validation without JavaScript.</span></span> 

<span data-ttu-id="d9ac5-141">Následující obrázek ukazuje, jak zakázat JavaScript v prohlížeči Firefox.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-141">The following image shows how to disable JavaScript in the Firefox browser.</span></span>

![Firefox: Na kartě Obsah možností zaškrtněte políčko Povolit Javascript.](~/tutorials/first-mvc-app/validation/_static/ff.png)

<span data-ttu-id="d9ac5-143">Následující obrázek ukazuje, jak zakázat JavaScript v prohlížeči Chrome.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-143">The following image shows how to disable JavaScript in the Chrome browser.</span></span>

![Google Chrome: V části Javascript v nastavení obsahu vyberte Nepovolit žádnému webu spouštět JavaScript.](~/tutorials/first-mvc-app/validation/_static/chrome.png)

<span data-ttu-id="d9ac5-145">Po zakázání Jazyka JavaScript zaúčtujete neplatná data a projděte ladicí program.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-145">After you disable JavaScript, post invalid data and step through the debugger.</span></span>

![Při ladění na post neplatných dat, Intellisense na ModelState.IsValid ukazuje, že hodnota je false.](~/tutorials/first-mvc-app/validation/_static/ms.png)

<span data-ttu-id="d9ac5-147">Část šablony zobrazení *Create.cshtml* je zobrazena v následujících značkách:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-147">The portion of the *Create.cshtml* view template is shown in the following markup:</span></span>

[!code-HTML[](~/tutorials/first-mvc-app/start-mvc/sample/MvcMovie22/Views/Movies/CreateRatingBrevity.html)]

<span data-ttu-id="d9ac5-148">Předchozí značky se používají metody akce k zobrazení počátečního formuláře a k jeho opětovnému zobrazení v případě chyby.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-148">The preceding markup is used by the action methods to display the initial form and to redisplay it in the event of an error.</span></span>

<span data-ttu-id="d9ac5-149">Pomocník [vstupních značek](xref:mvc/views/working-with-forms) používá [atributy DataAnnotations](/aspnet/mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-6) a vytváří atributy HTML potřebné pro ověření jQuery na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-149">The [Input Tag Helper](xref:mvc/views/working-with-forms) uses the [DataAnnotations](/aspnet/mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-6) attributes and produces HTML attributes needed for jQuery Validation on the client side.</span></span> <span data-ttu-id="d9ac5-150">Pomocná [pomoc s ověřovacíznačkou](xref:mvc/views/working-with-forms#the-validation-tag-helpers) zobrazuje chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-150">The [Validation Tag Helper](xref:mvc/views/working-with-forms#the-validation-tag-helpers) displays validation errors.</span></span> <span data-ttu-id="d9ac5-151">Další informace naleznete v [tématu Ověření.](xref:mvc/models/validation)</span><span class="sxs-lookup"><span data-stu-id="d9ac5-151">See [Validation](xref:mvc/models/validation) for more information.</span></span>

<span data-ttu-id="d9ac5-152">Co je opravdu pěkné o tomto přístupu je, že řadič ani šablona `Create` zobrazení neví nic o skutečné ověřovací pravidla jsou vynucena nebo o konkrétní chybové zprávy zobrazené.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-152">What's really nice about this approach is that neither the controller nor the `Create` view template knows anything about the actual validation rules being enforced or about the specific error messages displayed.</span></span> <span data-ttu-id="d9ac5-153">Ověřovací pravidla a chybové řetězce jsou `Movie` určeny pouze ve třídě.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-153">The validation rules and the error strings are specified only in the `Movie` class.</span></span> <span data-ttu-id="d9ac5-154">Tato stejná ověřovací pravidla `Edit` jsou automaticky použita pro zobrazení a všechny ostatní šablony zobrazení, které můžete vytvořit, které upravují váš model.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-154">These same validation rules are automatically applied to the `Edit` view and any other views templates you might create that edit your model.</span></span>

<span data-ttu-id="d9ac5-155">Když potřebujete změnit logiku ověření, můžete tak učinit přesně na jednom místě přidáním atributů ověření do modelu (v tomto příkladu `Movie` třída).</span><span class="sxs-lookup"><span data-stu-id="d9ac5-155">When you need to change validation logic, you can do so in exactly one place by adding validation attributes to the model (in this example, the `Movie` class).</span></span> <span data-ttu-id="d9ac5-156">Nebudete se muset starat o různé části aplikace, které nejsou v souladu s tím, jak jsou pravidla vynucena – veškerá logika ověření bude definována na jednom místě a použita všude.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-156">You won't have to worry about different parts of the application being inconsistent with how the rules are enforced — all validation logic will be defined in one place and used everywhere.</span></span> <span data-ttu-id="d9ac5-157">To udržuje kód velmi čistý a usnadňuje údržbu a vývoj.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-157">This keeps the code very clean, and makes it easy to maintain and evolve.</span></span> <span data-ttu-id="d9ac5-158">A to znamená, že budete plně ctít princip DRY.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-158">And it means that you'll be fully honoring the DRY principle.</span></span>

## <a name="using-datatype-attributes"></a><span data-ttu-id="d9ac5-159">Použití atributů datového typu</span><span class="sxs-lookup"><span data-stu-id="d9ac5-159">Using DataType Attributes</span></span>

<span data-ttu-id="d9ac5-160">Otevřete soubor *Movie.cs* `Movie` a zkontrolujte třídu.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-160">Open the *Movie.cs* file and examine the `Movie` class.</span></span> <span data-ttu-id="d9ac5-161">Obor `System.ComponentModel.DataAnnotations` názvů poskytuje kromě předdefinované sady atributů ověření také atributy formátování.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-161">The `System.ComponentModel.DataAnnotations` namespace provides formatting attributes in addition to the built-in set of validation attributes.</span></span> <span data-ttu-id="d9ac5-162">Již jsme použili `DataType` hodnotu výčtu pro datum vydání a pro pole cen.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-162">We've already applied a `DataType` enumeration value to the release date and to the price fields.</span></span> <span data-ttu-id="d9ac5-163">Následující kód ukazuje `ReleaseDate` `Price` vlastnosti a `DataType` s příslušným atributem.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-163">The following code shows the `ReleaseDate` and `Price` properties with the appropriate `DataType` attribute.</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc//sample/MvcMovie/Models/MovieDateRatingDA.cs?highlight=2,6&name=snippet2)]

<span data-ttu-id="d9ac5-164">Atributy `DataType` poskytují pouze rady pro modul zobrazení pro formátování dat (a poskytuje `<a>` prvky/atributy, například pro adresy URL a `<a href="mailto:EmailAddress.com">` e-mail.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-164">The `DataType` attributes only provide hints for the view engine to format the data (and supplies elements/attributes such as `<a>` for URL's and `<a href="mailto:EmailAddress.com">` for email.</span></span> <span data-ttu-id="d9ac5-165">`RegularExpression` Atribut můžete použít k ověření formátu dat.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-165">You can use the `RegularExpression` attribute to validate the format of the data.</span></span> <span data-ttu-id="d9ac5-166">Atribut `DataType` se používá k určení datového typu, který je konkrétnější než vnitřní typ databáze, nejsou ověřovací atributy.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-166">The `DataType` attribute is used to specify a data type that's more specific than the database intrinsic type, they're not validation attributes.</span></span> <span data-ttu-id="d9ac5-167">V tomto případě chceme pouze sledovat datum, ne čas.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-167">In this case we only want to keep track of the date, not the time.</span></span> <span data-ttu-id="d9ac5-168">`DataType` Výčet poskytuje mnoho datových typů, jako je například datum, čas, telefonní číslo, měna, e-mailová adresa a další.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-168">The `DataType` Enumeration provides for many data types, such as Date, Time, PhoneNumber, Currency, EmailAddress and more.</span></span> <span data-ttu-id="d9ac5-169">Atribut `DataType` může také povolit aplikaci automaticky poskytovat funkce specifické pro daný typ.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-169">The `DataType` attribute can also enable the application to automatically provide type-specific features.</span></span> <span data-ttu-id="d9ac5-170">Lze například `mailto:` vytvořit odkaz `DataType.EmailAddress`pro aplikaci a `DataType.Date` v prohlížečích podporujících html5 lze zadat volič data.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-170">For example, a `mailto:` link can be created for `DataType.EmailAddress`, and a date selector can be provided for `DataType.Date` in browsers that support HTML5.</span></span> <span data-ttu-id="d9ac5-171">Atributy `DataType` vyzařují atributy HTML 5 `data-` (vyslovuje se pomlčka dat), kterým prohlížeče HTML 5 rozumějí.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-171">The `DataType` attributes emit HTML 5 `data-` (pronounced data dash) attributes that HTML 5 browsers can understand.</span></span> <span data-ttu-id="d9ac5-172">Atributy `DataType` **neposkytují** žádné ověření.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-172">The `DataType` attributes do **not** provide any validation.</span></span>

<span data-ttu-id="d9ac5-173">`DataType.Date`neurčuje formát zobrazeného data.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-173">`DataType.Date` doesn't specify the format of the date that's displayed.</span></span> <span data-ttu-id="d9ac5-174">Ve výchozím nastavení je datové pole zobrazeno podle výchozích formátů založených na souborech serveru `CultureInfo`.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-174">By default, the data field is displayed according to the default formats based on the server's `CultureInfo`.</span></span>

<span data-ttu-id="d9ac5-175">Atribut `DisplayFormat` se používá k explicitnímu zadání formátu data:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-175">The `DisplayFormat` attribute is used to explicitly specify the date format:</span></span>

```csharp
[DisplayFormat(DataFormatString = "{0:yyyy-MM-dd}", ApplyFormatInEditMode = true)]
public DateTime ReleaseDate { get; set; }
```

<span data-ttu-id="d9ac5-176">Toto `ApplyFormatInEditMode` nastavení určuje, že formátování by mělo být použito také při zobrazení hodnoty v textovém poli pro úpravy.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-176">The `ApplyFormatInEditMode` setting specifies that the formatting should also be applied when the value is displayed in a text box for editing.</span></span> <span data-ttu-id="d9ac5-177">(To možná nechcete u některých polí , například u hodnot měny pravděpodobně nechcete, aby byl symbol měny v textovém poli pro úpravy určen.)</span><span class="sxs-lookup"><span data-stu-id="d9ac5-177">(You might not want that for some fields — for example, for currency values, you probably don't want the currency symbol in the text box for editing.)</span></span>

<span data-ttu-id="d9ac5-178">Atribut můžete `DisplayFormat` použít samostatně, ale je obecně vhodné použít `DataType` atribut.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-178">You can use the `DisplayFormat` attribute by itself, but it's generally a good idea to use the `DataType` attribute.</span></span> <span data-ttu-id="d9ac5-179">Atribut `DataType` vyjadřuje sémantiku dat na rozdíl od způsobu jejich vykreslení na obrazovce a poskytuje následující výhody, které nezískáte s DisplayFormat:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-179">The `DataType` attribute conveys the semantics of the data as opposed to how to render it on a screen, and provides the following benefits that you don't get with DisplayFormat:</span></span>

* <span data-ttu-id="d9ac5-180">Prohlížeč může povolit funkce HTML5 (například pro zobrazení ovládacího prvku kalendáře, symbolměny odpovídající národnímu prostředí, odkazy na e-mailatd.)</span><span class="sxs-lookup"><span data-stu-id="d9ac5-180">The browser can enable HTML5 features (for example to show a calendar control, the locale-appropriate currency symbol, email links, etc.)</span></span>

* <span data-ttu-id="d9ac5-181">Ve výchozím nastavení prohlížeč vykreslí data ve správném formátu na základě národního prostředí.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-181">By default, the browser will render data using the correct format based on your locale.</span></span>

* <span data-ttu-id="d9ac5-182">Atribut `DataType` může povolit MVC zvolit správnou šablonu pole `DisplayFormat` pro vykreslení dat (pokud používá sám používá šablonu řetězce).</span><span class="sxs-lookup"><span data-stu-id="d9ac5-182">The `DataType` attribute can enable MVC to choose the right field template to render the data (the `DisplayFormat` if used by itself uses the string template).</span></span>

> [!NOTE]
> <span data-ttu-id="d9ac5-183">jQuery ověření nefunguje s `Range` atributem a `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-183">jQuery validation doesn't work with the `Range` attribute and `DateTime`.</span></span> <span data-ttu-id="d9ac5-184">Například následující kód vždy zobrazí chybu ověření na straně klienta, i když je datum v zadaném rozsahu:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-184">For example, the following code will always display a client side validation error, even when the date is in the specified range:</span></span>
>
> `[Range(typeof(DateTime), "1/1/1966", "1/1/2020")]`

<span data-ttu-id="d9ac5-185">Chcete-li atribut použít s `Range` `DateTime`programem .</span><span class="sxs-lookup"><span data-stu-id="d9ac5-185">You will need to disable jQuery date validation to use the `Range` attribute with `DateTime`.</span></span> <span data-ttu-id="d9ac5-186">Obecně není vhodné kompilovat tvrdá data ve vašich `Range` modelech, takže použití atributu a `DateTime` je odrazováno.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-186">It's generally not a good practice to compile hard dates in your models, so using the `Range` attribute and `DateTime` is discouraged.</span></span>

<span data-ttu-id="d9ac5-187">Následující kód ukazuje kombinování atributů na jednom řádku:</span><span class="sxs-lookup"><span data-stu-id="d9ac5-187">The following code shows combining attributes on one line:</span></span>

[!code-csharp[](~/tutorials/first-mvc-app/start-mvc/sample/MvcMovie22/Models/MovieDateRatingDAmult.cs?name=snippet1)]

<span data-ttu-id="d9ac5-188">V další části série, zkontrolujeme aplikaci a provést některá vylepšení automaticky generované `Details` a `Delete` metody.</span><span class="sxs-lookup"><span data-stu-id="d9ac5-188">In the next part of the series, we review the app and make some improvements to the automatically generated `Details` and `Delete` methods.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="d9ac5-189">Další zdroje</span><span class="sxs-lookup"><span data-stu-id="d9ac5-189">Additional resources</span></span>

* [<span data-ttu-id="d9ac5-190">Práce s formuláři</span><span class="sxs-lookup"><span data-stu-id="d9ac5-190">Working with Forms</span></span>](xref:mvc/views/working-with-forms)
* [<span data-ttu-id="d9ac5-191">Globalizace a lokalizace</span><span class="sxs-lookup"><span data-stu-id="d9ac5-191">Globalization and localization</span></span>](xref:fundamentals/localization)
* [<span data-ttu-id="d9ac5-192">Úvod do pomocné spoje značek</span><span class="sxs-lookup"><span data-stu-id="d9ac5-192">Introduction to Tag Helpers</span></span>](xref:mvc/views/tag-helpers/intro)
* [<span data-ttu-id="d9ac5-193">Pomocné s tagy autora</span><span class="sxs-lookup"><span data-stu-id="d9ac5-193">Author Tag Helpers</span></span>](xref:mvc/views/tag-helpers/authoring)

> [!div class="step-by-step"]
> <span data-ttu-id="d9ac5-194">[Předchozí](new-field.md)
> [další](details.md)</span><span class="sxs-lookup"><span data-stu-id="d9ac5-194">[Previous](new-field.md)
[Next](details.md)</span></span>  
